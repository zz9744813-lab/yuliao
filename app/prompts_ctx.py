"""C/D 组实验 prompt 的正典（single source of truth）。

2026-09-14 从 scripts/phase15_gen.py 与 scripts/factorial_d.py 上收：
实验脚本原常量改为从这里 re-export，字节不变；Writer 接口（app/writer.py）
与实验生成路径共用同一份模板，杜绝口径漂移。prompt_version 保持不变
（recon_ctx_v1 / recon_ctxonly_v1），模板文本改动必须升版本。
"""

RECON_CTX_SYSTEM = "你是中文小说写作者，只输出正文。"
RECON_CTX_USER = """你在续写一部长篇小说。下面给出：前文（最近两段）、以及接下来这一段必须完成的骨架（Frame JSON）。

要求：
- 只输出"当前段"的正文，2~6 句，不重复前文
- 与前文的语流、指代、场景无缝衔接（不要重新起景、不要重复交代已知信息）
- 覆盖骨架的全部信息点；骨架中没有的信息不要添加
- 禁止复制骨架或前文中超过 6 字的原文连续字串

【前文-2】{prev2}

【前文-1】{prev1}

【当前段骨架】
{frame_json}

现在写"当前段"："""

CTXONLY_SYSTEM = "你是中文小说写作者，只输出正文。"
CTXONLY_USER = """你在续写一部长篇小说。下面是最近两段前文。

要求：
- 只输出"下一段"的正文，2~6 句，不重复前文
- 与前文的语流、指代、场景无缝衔接，自然推进剧情
- 不确定剧情走向时按最自然的方式推进；禁止复制前文超过 6 字的连续字串

【前文-2】{prev2}

【前文-1】{prev1}

写"下一段"："""
