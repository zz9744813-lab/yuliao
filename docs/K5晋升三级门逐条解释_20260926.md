# K5 晋升三级门逐条解释（2026-09-26）

任务 id：`lg-fix-promotion-gate-explain`｜kind：`code`｜
脚本：`scripts/k5_promotion_gate_explain.py`（只读，零库写、零模型调用、零 git 写）｜
回归：`tests/test_k5_promotion_gate_explain.py`（全离线，合成库，零真库依赖）

## 0. 这份文档回答什么

审计 P0-1/P0-2 至今 PARTIAL：8 条 legacy 策略全 `hypothesis`/`UNCERTAIN`、
`knowledge_packages=0`、83 条 `strategy_instances` 全 `verified` 但**非 benchmark
仅 1 条**。`scripts/k5_promotion_wire_probe.py` 已能机械回答「过门对数可不可核」，
但**不解释每条策略卡在哪一级门**。本件把这件事变成逐条可核证据：对每条策略
按 `app/knowledge_query.py` 的既有判据，**逐级报出判词**，并给出可机械执行的
收口判据清单。

**单源复用纪律（硬约束）**：门判据一律取自 `app/knowledge_query.py`
（`eligible_statuses` / `ELIGIBLE_OBSERVATION` / `_evidence_for` /
`_scope_matches` / `_condition_pipeline` / `ELIGIBLE_INSTANCE_STATUS` /
`DEFAULT_EXCLUDED_SOURCE_TYPES` / `DEFAULT_ALLOWED_TEXT_VERSIONS`），
**本件不另写一套判据**。见 §M4「单源复用清单」（机器区）。

### 0.1 真库只读实测结论（本轮实跑，空 policy）

一句话回答「8 条策略为何进不了 K3」：**它们在第 1 级就被剔出候选集，根本没走到
后三级**；而且**即使绕过第 1 级，它们仍会在第 3、4 级各被拦一次**（下面两组数字
是同一批行的两级独立读数，不矛盾）：

| 读数（真库 `mode=ro`，逐条见 §M1/§M2） | 值 | 出处 |
|---|---|---|
| 候选筛（status 门 + observation 门）过的条数 | **0 / 8** | `summary.n_beyond_status_gate` |
| 库侧落点 = `candidate_filtered_out`（既不在 `selected` 也不在 `rejected`） | **8 / 8** | §M5 对账表（解释器与 `query_knowledge` 两侧一致 8 条、不一致 0 条） |
| 假设不看第 1 级：第 3 级证据门净剩为 0 的条数 | **7 / 8** | `blocked_reason_per_gate.gate3_evidence` |
| 假设不看前三级：第 4 级 scope 判词为 `excluded_scope_uncertain` 的条数 | **8 / 8** | `blocked_reason_per_gate.gate4_scope` |
| 83 条实例里被基准段剔净的条数 | **82**（`benchmark_source`） | `summary.evidence.stripped_by_category` |
| 净剩非 benchmark 段 | **1**（根作品 `WK-6e5d2623`，仅 `legacy:少动作直给疑问`） | `summary.evidence.root_works` |
| 第 2 级 observation 门过的条数 | 8 / 8 | `summary.pass_per_gate`（**这一级不是卡点**） |
| 五级全通的条数 | **0** | `summary.n_all_gates_pass` |

读法（只陈述读数，不作裁定）：observation 门这一级 8/8 都过；真正把 8 条全拦在
候选集外的是 **status 门**（全 `hypothesis`，而 `eligible_statuses` 任何版本都只
含 `verified`）；证据侧的独立短板是 **83 条实例里只有 1 段不是基准段**；范围侧
8/8 是 `UNCERTAIN`。这三件事**互相独立**，任一件单独收口都不足以让任何一条进 K3
（对应 §M3 的 G1/G2、G4–G6、G7）。**该不该收口、收到什么范围，本件不裁定**（§1）。


## 1. 明确不做的事（任务书硬约束，逐条落死）

- **不裁定任何 `status` / `scope`**：只报既有门对既有行的判词（该过 / 该不过 /
  拒因是什么），不产出「应升格 / 应改范围」的意见。JSON 字段
  `discipline.no_status_or_scope_adjudication=true`。
- **不改任何一行数据**：连接层 `mode=ro`（`sqlite3.connect("file:...?mode=ro",
  uri=True)` 作 SQLAlchemy engine 的 creator，只读承诺在连接层成立，不靠自觉）；
  无 commit / add / DDL；零模型调用；零 git 写（不 commit / 不 merge / 不 push）。
- **不给「一键升格」建议**——那是策略审查席的事，本件不越界
  （`discipline.no_one_click_promotion_advice=true`）。
- **不放宽任何既有门**：policy 缺省为空（`{}`），不传任何 `source_policy`
  加严/放宽参数；`knowledge_query` 对来源类型/用途/文本版本三键是服务端封底
  （排除集并集、允许版本交集），调用方传什么都换不掉默认硬拦。
- **不碰原始语料**：不读 `segments.text` 参与判定；报告只含 id / span / 根作品
  id，不含任何原文（对齐 §4.2「包内容不含原文」）。
- **不猜**：库不可读 → `db_available=false` + 如实原因，逐级判词一栏空着并写明
  「本节无读数，不猜」。

## 2. 门序与「卡在哪一级」的口径

门序与 `KQ.query_knowledge` 的执行顺序**逐字对齐**（候选筛 status+observation
→ ①证据 → ②条件 → ③范围）：

| 序 | 门 | 判词来源（单源） | 判词字段 |
|---|---|---|---|
| 1 | `gate1_status` 第 1 级 status 门 | `eligible_statuses(version)` | `status ∈ 合格集` |
| 2 | `gate2_observation` 第 2 级 observation 门 | `ELIGIBLE_OBSERVATION` | `observation_status ∈ 合格集` |
| 3 | `gate3_evidence` 第 3 级证据门 | `_evidence_for` | `evidence_count` + `stripped` 拒因 |
| 4 | `gate_condition` 条件管道（随附，不另编号） | `_condition_pipeline` | 拒绝理由原样 |
| 5 | `gate4_scope` 第 4 级 scope 门 | `_scope_matches` | 判词原样（`UNCERTAIN` → `excluded_scope_uncertain`） |

- `blocked_at`（卡在哪一级）= **第一条不过的级**（不跳级猜；多级皆红时只报最早
  那条，其余照样逐级报出——解释器要的是可核证据，不是短路日志）。
- 每一级的 `reason_source` 字段标出判词来源：`library`＝判词字面量取自
  `knowledge_query`；`explainer`＝**判据单源、但库不产出该判词**（status/obs 两级
  在 `query_knowledge` 的候选筛里内联过滤，只产 pass/fail，不产判词）。
- stripped 拒因**只做字符串归并**（判词首段 → 分类键），不改判词；分类表未收录
  的判词落 `unknown:*` 并单列（fail-visible，不静默归并）。

## 3. 逐字单源自证（不是人工比对）

解释器自己知道「判据是否被偷偷改宽/改窄」——它每跑一次都用**同一个 policy**
现场复跑一次 `KQ.query_knowledge`（只读），逐条比对本解释器的落点与库侧落点
（`selected` / `rejected:<reason>` / `candidate_filtered_out`），不一致即
`library_crosscheck.n_disagree > 0`。回归对这条钉死（`n_disagree == 0`），
并另有一条用例喂「与库侧不符的假解释」证明该对账器本身可证伪。

## 4. 反向验证（真做过一次变异，跑完即恢复）

**方法**：把 `scripts/k5_promotion_gate_explain.py:191` 的 status 门判据
`g1_pass = st.status in eligible` 原地改成恒真（`g1_pass = True`），跑同一门
回归看是否立刻变红；随后**按字节恢复**（sha256 比对），复跑看是否复绿。

| 步骤 | 期望 | 实测 |
|---|---|---|
| 变异后跑 `pytest tests/test_k5_promotion_gate_explain.py -q` | 变红（exit≠0） | **exit 1**，5 条用例红：`test_status_gate_uses_version_bucket` / `test_four_block_points_plus_all_pass` / `test_blocked_at_is_first_failing_gate_not_last` / `test_library_crosscheck_agrees_on_all_five_shapes` / `test_closure_criteria_are_mechanical` |
| 恢复后跑同一门 | 复绿（exit 0） | **exit 0**，16 passed |
| 恢复后文件 sha256 | 与变异前逐字一致 | 一致（`17afb053…b758` → `17afb053…b758`，`restored_byte_identical=True`） |
| 变异残留标记清零 | 本件交付文件内计数 = 0 | **0 / 0 / 0**（三个交付文件逐个点名；全树另有 5 行**早于本任务**存在的核对命令原文，非本件残留——逐行点名见 §6） |

5 条红覆盖三类断言面：①逐级判词方向（status 门恒真后 `blocked_at` 从
`gate1_status` 漂到别的级）；②`blocked_at`「第一条不过的级」；③与库侧的
逐字对账（库侧仍判 `candidate_filtered_out`，解释器却放行 → disagree）。
即：这道回归对「status 门被放宽」是**真敏感**的，不是摆设。

## 5. 复跑方式（主控可逐字核对）

```bash
# 只打印，零写盘（不落任何文件）
F:/Hermes/hermes-agent/venv/Scripts/python.exe scripts/k5_promotion_gate_explain.py --print-only

# 默认档：只重写本文档的机器区（§M1–§M6，标记之间），叙述段原样保留
F:/Hermes/hermes-agent/venv/Scripts/python.exe scripts/k5_promotion_gate_explain.py

# 回归门（合成库，不碰真库）
F:/Hermes/hermes-agent/venv/Scripts/python.exe -m pytest tests/test_k5_promotion_gate_explain.py -q
```

- 库路径解析：`--db` 覆盖 → `<repo>/data/language_genome.db` → 候选绝对路径
  `F:/agi/language-genome/data/language_genome.db`（worktree 检出不带 `data/` 时
  仍能**只读**实测主仓真库；主控在主仓 cwd 复跑时命中第一条）。
- 缺省 policy = `{}`（空 policy，无查询作品）——与主控 2026-09-26 真库实测口径
  逐字一致。要按某本书复跑加 `--book-id <id>` 即可（`--policy-json` 可给
  policy 文件，但**只能加严、不能放宽**服务端硬拦）。
- 本文档 §M 区由脚本重生成，幂等：同库复跑只换时间戳。
- 在主仓 cwd 复跑时：库命中 `<主仓>/data/language_genome.db`，缺省会把同名单文件
  写进 `<主仓>/docs/`（叙述段不存在时生成「标题+机器区」骨架）——**若不想在主仓
  落盘，请加 `--print-only`**（逐策略判词与本文档 §M1 逐字相同，可用
  `--json-out` 落到仓外）。

## 6. 反向验证的残留核对（变异标记清零）

变异标记串在工作树内的出现次数必须为 0（不写死字面量，用 shell 拼出来核对，
免得核对命令自己把标记写进树里）：

```bash
grep -rc -- "$(printf 'MUTA%s' 'TION')" . --exclude-dir=.git --exclude-dir=__pycache__ \
  | grep -v ':0$' || echo "计数=0（无变异残留）"
```

实测（2026-09-26，本件交付后）：

- 本件三个交付文件内计数 = **0 / 0 / 0**（脚本已按字节恢复，见 §4 的 sha256
  对账；测试文件与本文件从未写过该标记）。
- 全树（含 5 个**早于本任务**已存在的文件）计数 = 5 行，逐行点名如下，**都不是
  本次变异残留**，而是同族件（`k5_promotion_wire_probe` / 盲呈现 / nsent 方向门
  各自的反向验证记录）当年**主动写进文档的核对命令原文**：
  `docs/K5晋升接线探针_20260925.md:167`、`PORT_promotion_wiring_probe.md:46`、
  `PORT_blind_concurrency.md:236/252/253`、
  `PORT_nsent_direction_gate.md:264/265`、`REVIEW_PROBE_LEDGER.md:113`
  （以上 5 个文件均**不在**本件允许编辑清单内，本件**不改动**它们；此处点名只为
  免得主控核对时误判为本件残留）。

## 7. 收口判据（摘要；完整表与逐条核对式见 §M3）

§M3 的 G1–G8 每条都带**当前读数 + 只读 SQL 或调用式 + 机械判定**，阈值一律标
`target_is_placeholder=true`：存在性下界 1（或 G5 的 2）是**占位阈值**，真实
阈值属策略审查席裁定范围——**本件只给机械读数，不代裁**。

- 判据是**机械可执行**的（G1/G2/G3/G5-SQL 是纯只读 SQL；G4/G6/G7/G8 走
  `_evidence_for` / `_scope_matches` 调用式，因为镜像去重与范围比对没有纯 SQL
  等价式）。
- G5-SQL 里的值域（`ELIGIBLE_INSTANCE_STATUS` / `DEFAULT_EXCLUDED_SOURCE_TYPES`
  / `DEFAULT_ALLOWED_TEXT_VERSIONS`）由脚本**按运行期从 `knowledge_query`
  常量展开**写入，未手抄。
- 当前读数里 G3（observation 门）与 G4（至少 1 条策略证据净剩非空）**已达标**，
  G1/G2/G5/G6/G7/G8 未达标——**「未达标」是读数，不是判决**（升不升格、收到什么
  范围是策略审查席的事）。

## 8. `未自跑` 与未实跑段落的标注纪律

- 本文档的 **§M1–§M6 全部是实跑产物**（真库 `mode=ro` 现场读数，生成时间见
  §M 区首行注释）。
- §4 反向验证的两次运行（变异红 / 恢复绿）、§6 的残留计数**均为实跑**，已把
  exit 码与 sha256 记在表里。
- **未自跑的段落**：本任务**没有**做的事一律在此点名，不许把推演写成实跑——
  ① 「哪些策略该升格 / 该定什么 scope」——**未跑、也不该由本件跑**（属策略审查席）；
  ② 带查询作品（`--book-id`）的 scope 判词复跑——本轮真库实测用的是空 policy，
  故 8 条 scope 判词只对 `UNCERTAIN` 是完整可解释的（`UNCERTAIN` 无论有无查询
  作品都返回 `excluded_scope_uncertain`）；若将来有卡带非 UNCERTAIN scope，空
  policy 下 `_scope_matches` 会因「无查询作品」返回 `excluded_scope`，该读数
  **不能**当作该卡的范围裁定——`未自跑`，需带 `--book-id` 复跑；
  ③ 收口判据的真实阈值——**未跑**（无拍板输入，故只给占位阈值）；
  ④ K3-B `freeze_package` 落包与 K4 preflight 的下游段——**未跑**（本件只解释
  门判词，不跑下游写入流程）。
- 临时产物（合成库、临时 JSON）全部在 `tmp_path` / 系统临时目录内，用例结束即
  随 pytest 临时目录回收；本件**不在仓内留任何 scratch / smoke / repro 脚本**。

## 9. 与既有件的关系

- `scripts/k5_promotion_wire_probe.py`：报「过门对数可不可核 / 写入路径在哪 /
  链断在哪一段」；**不解释逐条策略卡在哪一级门**——本件补的就是这一段，且复用
  同一套 `app/knowledge_query.py` 判据。
- `scripts/k5_criteria_check.py::promotion_gap_preflight`：只答「卡未晋升 / K3
  过滤 / 不足证据」四态分流；本件给出该四态背后的**逐级判词与拒因计数**。
- `scripts/k2_rereview_queue.py`：83 条 legacy 实例的复审队列（默认只读档）——
  本件的 83 条实例按策略归类后的拒因分布（82 条 `benchmark_source`）与之对账。

<!-- BEGIN: k5_promotion_gate_explain (machine-generated) -->

<!-- 本段由 scripts/k5_promotion_gate_explain.py 生成于 2026-09-26T10:37:56+08:00；库 <outside-repo>/language_genome.db（db_mode=ro，opened=True）；勿手改，复跑即重生成 -->

### M1 逐策略逐级判词（8 条）

门序（与 `KQ.query_knowledge` 执行顺序对齐）：`gate1_status` → `gate2_observation` → `gate3_evidence` → `gate_condition` → `gate4_scope`；`卡在哪一级` 取**第一条不过的级**。

| strategy_key | version | ①status | ②obs | ③证据净剩 | ③拒因分类计数 | 条件判词 | ④scope 判词 | 卡在哪一级 |
|---|---|---|---|---|---|---|---|---|
| legacy:保留原句的递进铺陈 | 1 | `hypothesis` ❌ | `observed` ✅ | 0 段/区间（根作品 —） | `benchmark_source`=10 | `pass` | `excluded_scope_uncertain`（scope=UNCERTAIN） | **gate1_status** |
| legacy:克制概述vs铺陈渲染 | 1 | `hypothesis` ❌ | `observed` ✅ | 0 段/区间（根作品 —） | `benchmark_source`=11 | `pass` | `excluded_scope_uncertain`（scope=UNCERTAIN） | **gate1_status** |
| legacy:克制留白vs扩写解释 | 1 | `hypothesis` ❌ | `observed` ✅ | 0 段/区间（根作品 —） | `benchmark_source`=10 | `pass` | `excluded_scope_uncertain`（scope=UNCERTAIN） | **gate1_status** |
| legacy:克制直写对话动作 | 1 | `hypothesis` ❌ | `observed` ✅ | 0 段/区间（根作品 —） | `benchmark_source`=9 | `pass` | `excluded_scope_uncertain`（scope=UNCERTAIN） | **gate1_status** |
| legacy:克制直陈vs铺陈注水 | 1 | `hypothesis` ❌ | `observed` ✅ | 0 段/区间（根作品 —） | `benchmark_source`=9 | `pass` | `excluded_scope_uncertain`（scope=UNCERTAIN） | **gate1_status** |
| legacy:少动作直给疑问 | 1 | `hypothesis` ❌ | `observed` ✅ | 1 段/区间（根作品 ['WK-6e5d2623']） | `benchmark_source`=11 | `pass` | `excluded_scope_uncertain`（scope=UNCERTAIN） | **gate1_status** |
| legacy:直写本体短推进 | 1 | `hypothesis` ❌ | `observed` ✅ | 0 段/区间（根作品 —） | `benchmark_source`=10 | `pass` | `excluded_scope_uncertain`（scope=UNCERTAIN） | **gate1_status** |
| legacy:直抒情绪vs铺陈演绎 | 1 | `hypothesis` ❌ | `observed` ✅ | 0 段/区间（根作品 —） | `benchmark_source`=12 | `pass` | `excluded_scope_uncertain`（scope=UNCERTAIN） | **gate1_status** |

### M2 汇总计数（机器读数，逐项可复算）

| 项 | 值 |
|---|---|
| 策略总数 n_strategies | 8 |
| status 分布 | {"hypothesis": 8} |
| observation_status 分布 | {"observed": 8} |
| scope 分布 | {"UNCERTAIN": 8} |
| 过第 1/2 级（候选筛）条数 | 0 |
| 各级通过条数 | {"gate1_status": 0, "gate2_observation": 8, "gate3_evidence": 1, "gate_condition": 8, "gate4_scope": 0} |
| 卡点分布（blocked_at） | {"gate1_status": 8} |
| 五级全通条数 | 0 |
| 证据：净剩区间数 | 1 |
| 证据：考虑实例数 | 83 |
| 证据：被剔除实例数 | 82 |
| 证据：拒因分类计数 | {"benchmark_source": 82} |
| 证据：拒因分类×取值 | {"benchmark_source": 82} |
| 证据：去重后独立根作品数 | 1 |
| 证据：净剩非空的策略条数 | 1 |

**拒因分类口径**（`_evidence_for` 产出的判词 → 分类；**只归并不判定**，判词本体逐字来自库）：

| 分类键 | 说明 |
|---|---|
| `no_registry` | 来源未登记（work_sources 无该 work 行） |
| `benchmark_source` | 基准段剔除（segments.role=benchmark，基准上下文泄漏） |
| `excluded_source_type` | 来源类型被排除（fixture/synthetic/commentary 等，附具体类型） |
| `excluded_use` | license 禁用用途（license_purposes 撞 excluded_uses） |
| `text_version` | 文本版本不在允许集（附具体版本） |
| `mirror_dedup` | 同（根作品, span）区间重复/镜像，不计独立 |
| `cross_work` | 跨作品（require_same_book 生效时不保留） |
| `query_book_no_registry` | 查询作品无登记行（require_same_book 无从比对） |

- 分类表未收录的判词（fail-visible，不静默归并）：**无**

### M3 可机械执行的收口判据清单（只给读数与核对式，**不裁定**）

| id | 判词 | 阈值 | 当前读数 | 机械判定 | 只读 SQL / 调用式 |
|---|---|---|---|---|---|
| G1 | 至少 1 张 status 落在 eligible_statuses(version) 的卡 | 1 | 0 | 未达标 | `SELECT COUNT(*) FROM expression_strategies_v2 WHERE status IN ('verified')` |
| G2 | 至少 1 张卡 status<>'hypothesis' 且 scope<>'UNCERTAIN' | 1 | 0 | 未达标 | `SELECT COUNT(*) FROM expression_strategies_v2 WHERE status<>'hypothesis' AND scope<>'UNCERTAIN'` |
| G3 | 至少 1 张卡 observation_status 落在 ELIGIBLE_OBSERVATION | 1 | 8 | 达标 | `SELECT COUNT(*) FROM expression_strategies_v2 WHERE observation_status IN ('observed','replicated')` |
| G4 | 至少 1 条策略 _evidence_for 的 evidence_count ≥ 1（证据净剩非空） | 1 | 1 | 达标 | `python scripts/k5_promotion_gate_explain.py --print-only → strategies[].gates.gate3_evidence.evidence_count（= KQ._evidence_for 的第 2 个返回值）` |
| G5 | 非 benchmark 且过来源闸的净剩段数 ≥ 2（去重后独立根作品口径） | 2 | 1 | 未达标 | `见 G5-SQL（只读；与 _evidence_for 的去重口径并用）` |
| G6 | 至少 1 条策略不被 benchmark_source 剔空（剥离后仍有候选实例） | 1 | 0 | 未达标 | `python scripts/k5_promotion_gate_explain.py --print-only → strategies[].gates.gate3_evidence.stripped.by_category（= KQ._evidence_for 第 3 个返回值的归并）` |
| G7 | 至少 1 条策略 scope 判词为 pass（_scope_matches） | 1 | 0 | 未达标 | `python scripts/k5_promotion_gate_explain.py --print-only → strategies[].gates.gate4_scope.reason（= KQ._scope_matches 的返回值）` |
| G8 | 至少 1 条策略五级全通（blocked_at 为空） | 1 | 0 | 未达标 | `python scripts/k5_promotion_gate_explain.py --print-only → summary.n_all_gates_pass` |
| G5-SQL | 只读 SQL：非 benchmark、status∈ELIGIBLE_INSTANCE_STATUS、来源类型不在排除集、文本版本在允许集的实例行数（按策略参数化 strategy_id） | None | None | （核对式） | `SELECT COUNT(*) FROM strategy_instances si JOIN segments sg ON sg.id = si.segment_id JOIN work_sources ws ON ws.work_id = si.work_id WHERE si.status IN ('ver…`（完整式见 JSON closure_criteria） |

> 阈值说明：存在性下界 1（或 2）为占位阈值——真实阈值属策略审查席裁定范围，本报告只给机械读数，不代裁

### M4 单源复用清单（判据本体全部取自 `app/knowledge_query.py`）

| 门 | 复用符号 | 类型 | 用途 |
|---|---|---|---|
| gate1_status | `eligible_statuses(version)` | function | 第 1 级 status 门判据 |
| gate2_observation | `ELIGIBLE_OBSERVATION` | constant | 第 2 级 observation 门判据 |
| gate3_evidence | `_evidence_for(s, id, policy)` | function | 第 3 级证据门的 evidence_count 与 stripped 判词 |
| gate_condition | `_condition_pipeline(s, id, req)` | function | 条件管道判词（随附，不另编号） |
| gate4_scope | `_scope_matches(s, strategy, policy)` | function | 第 4 级 scope 门判词 |
| evidence_side | `ELIGIBLE_INSTANCE_STATUS` | constant | 证据侧 status 口径（对账用） |
| evidence_side | `DEFAULT_EXCLUDED_SOURCE_TYPES` | constant | 来源类型排除集（对账用） |
| evidence_side | `DEFAULT_ALLOWED_TEXT_VERSIONS` | constant | 文本版本允许集（对账用） |
| crosscheck | `query_knowledge(policy, s)` | function | 单源自证：现场复跑并逐条比对落点 |

- 本地另写的判据：none（本文件不定义任何合格集/判据；所有门判据均引用上面的符号）
- 本地仅做组合（不改判据本体）：gate1/gate2 的 reject 理由串（库在候选筛里内联过滤，不产出判词）；stripped 判词的分类归并（只归并字符串，不改判词）

### M5 逐字单源自证（复跑 `KQ.query_knowledge` 对账）

- `query_knowledge.status` = `empty`；一致 8 条 / 不一致 0 条。

| strategy_key | 解释器落点 | 库侧落点 | 一致 |
|---|---|---|---|
| legacy:保留原句的递进铺陈 | `candidate_filtered_out` | `candidate_filtered_out` | ✅ |
| legacy:克制概述vs铺陈渲染 | `candidate_filtered_out` | `candidate_filtered_out` | ✅ |
| legacy:克制留白vs扩写解释 | `candidate_filtered_out` | `candidate_filtered_out` | ✅ |
| legacy:克制直写对话动作 | `candidate_filtered_out` | `candidate_filtered_out` | ✅ |
| legacy:克制直陈vs铺陈注水 | `candidate_filtered_out` | `candidate_filtered_out` | ✅ |
| legacy:少动作直给疑问 | `candidate_filtered_out` | `candidate_filtered_out` | ✅ |
| legacy:直写本体短推进 | `candidate_filtered_out` | `candidate_filtered_out` | ✅ |
| legacy:直抒情绪vs铺陈演绎 | `candidate_filtered_out` | `candidate_filtered_out` | ✅ |

### M6 本次运行参数与纪律

| 项 | 值 |
|---|---|
| db_path | `<outside-repo>/language_genome.db` |
| db_mode | `ro` |
| db_opened | True |
| policy（本次判词所依据的查询 policy） | `{}` |
| 零库写 / 零模型调用 / 零 git 写 | 0 / 0 / 0 |
| 裁定 status 或 scope | 未做（`discipline.no_status_or_scope_adjudication=true`） |
| 一键升格建议 | 未给（属策略审查席） |

<!-- END: k5_promotion_gate_explain (machine-generated) -->
