# K5 判据核验与十场路径 dry-run（2026-09-25，lg-k5-criteria-check）

- 工具：`scripts/k5_criteria_check.py`（只读、零写库、零模型调用；十场
  dry-run 走 `scripts/k4_paired_scenes.py --scenes 10` 的**离线 FixtureClient**
  路径——零真实调用、freeze=False 零库写，产物落临时目录用后即清）。
- 判据来源：docs/K5-A_离线前置评估.md（v1/2）P0–P3 / C1–C5 / §4 止损。
- **本报告是 dry-run 与只读核验：不构成任何判据的通过证据。**

## 0. 实跑命令与退出码

```
F:/Hermes/hermes-agent/venv/Scripts/python.exe scripts/k5_criteria_check.py \
  --repo-root F:/agi/language-genome \
  --out k5_report_20260925.json
→ CHECK_EXIT=0
```

验收命令（任务书口径）：

```
F:/Hermes/hermes-agent/venv/Scripts/python.exe -m pytest \
  tests/test_k5_criteria_check.py -q
→ 5 passed，exit 0
```

## 1. 判据核验总览（真跑输出）

- 收据源（自动取最新）：`out_k4_3_mc22_v2/k4_paired.json`
- **k5_established = False**（恒；dry-run 永不判已通——测试红线钉死）
- verdict：**K5 未通**——dry-run 与只读核验不构成通过；当前判据 **2/10**
  项机械层满足（P0/C4+；C5 经会审整改后由 ✅ 改判 ❌），C1 人工复核结构性缺证据。

| 判据 | 满足 | 证据（真跑读出） | 缺什么 |
|---|---|---|---|
| P0 通道健康 | ✅ | live=True，committed=2 处（mc22_v2 收据实证通道可用） | — |
| P1 三场 6/6 臂 committed | ❌ | **committed=2/6；failures=2；skipped=2** | 缺「三场全过」真跑收据（s2 两臂被预算/改稿闸诚实拒） |
| P2 A 臂非空包率 ≥2/3 | ❌ | **n_techniques≥1 场数=0（逐场 [0,0]）** | 知识卡未晋升：24 席判定全否（docs/策略语义审查_判定模型版_20260923.md），S1/S2 成对证据→两席判定→status 晋升链未走完；K3 只服务 verified+observed（不许放宽） |
| P3 双闸纪律 | ⚠️ 机制层 | --live 双闸 + test_live_double_gate 回归在位；收据 live=True 只能经门产生 | 「未被绕过」离线不可全证，真值待主控审计 |
| C1 人工复核 ≥1 处 | ❌ | 机械层无人工复核记录可读（设计上只认人工证据） | **结构性缺证据**：离线校验永远给不出，K5 不可能由 dry-run 宣告通过 |
| C2 零失败臂 | ❌ | **failures=2**（rollback_failed 条目 0） | s2 失败臂须修卡/调参后重跑 K4，不扩 10 场 |
| C3 A 臂非空包率 ≥2/3 | ❌ | n_techniques≥1 场数=0 | 同 P2 根因 |
| C4 复核负担 ≤20 分钟/场 | ❌ | 无差异清单与复核耗时记录 | 依赖 C1 人工复核先行 |
| C5 通道一致性 | ❌ | **channel_changed=True**；writer/verifier=deepseek-v4.1-flash（收据逐条带通道/模型留痕） | **10 场收据缺位（无 `out_k4_10*/k4_paired.json`）**：C5 是「10 场与三场同通道」的一致性判据，只有三场收据不能判过（会审 2026-09-25 指出原判 ✅ 与文档自身「缺什么」列自相矛盾，已改判并加机械前提） |
| C4+ 止损 ≤800 万 | ✅ | 收据口径 tokens=2,167（**仅 committed 臂**）＋世界库口径 12,810（含失败臂，2 个 arm 库逐 job 对账）⇒ 止损取两者**较大者** 12,810 | — |

## 2. 十场路径 dry-run（逐场表格，离线真跑输出）

离线跑 `--scenes 10`（FixtureClient、真库只读）：**10 场 × 2 臂结构性
20/20 committed，failures=0，skipped=0**——机制层十场路径通畅。**真跑时
每一场都会被下列判据拦下**（拦的是证据链，不是机制）：

| 场 | 离线结构态 | 预计预算（正常→最坏） | 预计 tokens（801/调用实测单价） | 真跑会被谁拦 |
|---|---|---|---|---|
| s1–s10（逐场同构） | A/B 双 committed | 4 调用 → 12 调用/场 | ≈3,204 → ≈9,612/场 | **C3/P2**（A 臂包空：卡未晋升）、**C5**（10 场收据缺位须同通道再标）、**C1**（人工复核结构性缺）、**P1/C2**（三场基线尚未 6/6） |
| 合计 | 20/20 | **40 → 120 调用** | **≈32,040 → ≈96,120**（≪ 止损 800 万） | — |

单价口径（如实双列）：三场真跑实测 ≈**801 tokens/调用**（12,810 tokens/
16 调用＝世界库 calls 行数，含失败链；**由最新收据实算，不再硬编码**）；K5-A §4 文档口径按每调用 4~6 万 token 的语料级估算
（160~240 万正常/480~720 万最坏）——**扩场前以实测复核为准**；货币单价
以通道账为准，不虚构。

## 3. 三条最可能卡住的点（含证据行）

1. **C3/P2：A 臂知识包全空——策略卡晋升链未走完（当前第一卡点）**
   证据行：`"n_techniques≥1 场数=0"`（本核验器真跑输出）；根因链：
   docs/策略语义审查_判定模型版_20260923.md（24/24 判证据不支持，0 条
   verified）→ docs/策略卡合并方案_20260923.md（S1/S2 合并卡）→
   成对对照证据已生成（`_pairs/k2_pairs_20260924.json`，48 对、
   35/48 过六门——docs/K2_重抽_成对对照_20260924.md）**但两席独立判定
   与 status 晋升尚未执行**——K3 只服务 verified+observed，不许放宽。
2. **P1/C2：三场基线尚未 6/6 committed**
   证据行：`"committed=2/6；failures=2；skipped=2"`（out_k4_3_mc22_v2）：
   s2 A 臂 6 调用后 call_budget_exhausted、s2 B 臂 rewrite_budget_
   exhausted（verifier 三轮不放行）——真跑扩 10 场前必须先修到三场全过。
3. **C1：人工复核结构性缺证据（永远不能由 dry-run 给出）**
   证据行：`"缺集霸（或其授权复核席）对 A/B 差异的人工复核记录"`——
   该判据设计上只认人工证据；即使前两条全部修好，K5 通过仍需集霸侧
   ≥1 处方向差异的人工复核 + C4 复核负担记录。**本工具与十场 dry-run
   在任何情况下都不构成 K5 通过。**

## 4. 仍得不到什么（红线声明）

- **K5 未通**：3/10 机械满足 ≠ 通过；本核验是 dry-run + 只读，不产生
  任何判据要求的真跑证据（十场 live 收据、人工复核记录、策略卡晋升）。
- 十场真跑的实耗与判据终值：须待卡晋升链与三场基线修复后，由派工方
  另行授权 `--live`（本工具不提供、不触发任何 live 路径）。


## 5. 会审整改（2026-09-25 15:1x，针对推送门禁 BLOCK 的六条）

会审（glm-5.3 席 PASS 但列 11 条、qwen3.8-flash 席未按 PASS/BLOCK 收口 ⇒ 全票通过制判 BLOCK）
指出的实质项与整改状态（逐条可复核）：

| # | 会审意见 | 状态 | 实证 |
|---|---|---|---|
| 1 | C5 在 10 场收据缺位时不得判 `satisfied=True`（判据表自相矛盾） | ✅ 已修 | 新增 `has_ten_scene_artifact()`（`out_k4_10*/k4_paired.json`）作为 C5 机械前提；本报告 C5 已由 ✅ 改判 ❌，missing 明写「10 场收据缺位」 |
| 2 | 止损只累加 `receipts[].usage.tokens` 会系统性少计（2,167 vs 12,810） | ✅ 已修 | 新增 `_world_db_tokens()`：只读打开 `arm*/*.sqlite` 的 `calls.response` 逐 job 累加 `tokens_in+tokens_out`（含失败臂），止损取 `max(收据, 世界库)`；证据行双口径并列披露；库不可读时**如实降级并写明原因**（不静默当 0，另有回归钉死） |
| 3 | 十场 dry-run 全量继承父环境，`K4_ALLOW_LIVE` 等残留会透传；只看 returncode 不断言真离线 | ✅ 已修 | 显式 `env.pop` 掉 `K4_ALLOW_LIVE`/`LG_LIVE`/`LG_LLM_MODE`；并断言子进程产物 `live is False`，否则中止并落 `ok=False`（回归钉死） |
| 4 | `subprocess.TimeoutExpired` 未捕获 ⇒ 裸异常栈崩，违背「缺证据必须如实报未满足」 | ✅ 已修 | 捕获后落 `{"ok": False, "error": "…超时…"}`（回归钉死） |
| 5 | `measured_per_call = 542` 硬编码魔数，收据更新后脱节 | ✅ 已修 | 改为从最新三场收据实算：`12810 tokens / 16 调用（世界库 calls 行数）≈ 801`；无收据时用显式 `fallback_per_call` 并在 `price_src` 写明来源 |
| 6 | `blockers` 硬编码字符串，与判据表脱节 | ✅ 已修 | 改为由 `check_criteria()` 实际结果派生 |
| 7 | P1 只核对 `len(committed)==6` 总数，不校验三场每场 2/2 分布 | ⏳ **未修（如实记）** | 仍只校验总数；当前真值 committed=2/6 已判 ❌，不影响本次结论；列入下一批整改 |
| 8 | 报告 JSON 落仓根 + `--repo-root` 硬编码机器路径 | ⏳ 未修（如实记） | 本次只做判据与安全类整改；路径可移植性列入下一批 |

- 回归：`tests/test_k5_criteria_check.py` 由 5 例扩到 **10 例**（新增世界库口径/不可读降级/C5 十场前提/models 缺键/超时/live 断言/清闸共 5 例），
  主控实跑 **10 passed，exit 0**。
- 判据结论**未放宽**：`k5_established` 恒 `False`；整改后机械满足由 3/10 变为 **2/10**（C5 从严），红线保持。


## 6. 遗留整改 A/B（2026-09-25 17:0x，lg-fix-k5-residual / 分支 `fix/k5-residual`，基线 `ddf14ce`）

本节对应 §5 表里如实记为「未修」的两条（会审第 7、8 条）。本批只动
`scripts/k5_criteria_check.py`、`tests/test_k5_criteria_check.py` 与本节文档：
未动 `app/`、`data/`、其它测试与文档；未写库、未 `--live`、未调用任何模型。

| # | 遗留项 | 状态 | 落地的口径 |
|---|---|---|---|
| A（第 7 条） | P1 只核 `len(committed)==6` 总数，不校验「三场每场 2/2」分布 | ✅ 已修 | P1 改**按 scene 分桶校验**：新增 `K4_REQUIRED_SCENES=(s1,s2,s3)` / `K4_REQUIRED_ARMS=("A","B")` 与 `_committed_by_scene()` / `_committed_arms()`。满足条件＝ `live` ∧ 三场各自 A、B 两臂都 committed ∧ 逐场计数各＝2 ∧ 无三场口径外的场 ∧ 总数恰 6 ∧ `failures=0`。因此「6 处集中在少数场」和「committed>6（含 4 场收据、10 场收据被 `out_k4_3*` glob 误匹配）」两种形态**都不判过**（旧口径前者会被总数骗过）。证据行新增 `per_scene={'s1': 2, 's2': 0, 's3': 0}` 与逐场臂集合，缺哪一臂在 `missing` 里逐条指名 |
| B（第 8 条） | `--repo-root` 缺省硬编码 `F:/agi/language-genome`；报告内嵌绝对路径；`generated_at` 无时区；报告落仓根 | ✅ 已修（旧报告搬迁超范围，见 6.3） | ① `--repo-root` 缺省＝脚本自身推导的 `ROOT`（显式传入仍生效；参数构造拆出 `build_argparser()` 以便回归钉死）；② 新增 `portable_path()`：`k4_artifact` 与 P0 证据行的路径一律**相对仓库根**（如 `out_k4_3_x/k4_paired.json`），仓库根之外的路径退化为 `<仓外>/文件名`，报告里不出现盘符；③ `generated_at` 改为 `datetime.now(timezone.utc).astimezone().isoformat(timespec="seconds")` ⇒ ISO8601 带时区偏移（本机 `+08:00`）；④ 新增 `resolve_out_path()`：`--out` 给**裸文件名一律落 `<repo>/docs/`**（与 `docs/judge-debias-probe2-20260920.json` 同惯例），仓根不再新增报告；带目录的相对路径按仓库根解析，绝对路径按给定写入 |

### 6.1 新命令（取代 §0 的历史命令；§0 保留为当轮实跑快照）

```
F:/Hermes/hermes-agent/venv/Scripts/python.exe scripts/k5_criteria_check.py \
  --repo-root F:/agi/language-genome \
  --out k5_report_20260925.json
→ 裸文件名 ⇒ 报告实际落位 docs/k5_report_20260925.json
```

- `--repo-root` 现在可省略（缺省＝脚本所在仓库根）；跨树核验主仓收据时仍建议显式传入。
- 报告内的 `k4_artifact` / P0 证据行改读为**相对仓库根**，`generated_at` 带 `+08:00` 偏移。

### 6.2 回归与实跑状态（如实）

回归由 §5 的 10 例扩到 **16 例**，本批新增 6 例：

| 新用例 | 钉什么 |
|---|---|
| `test_p1_requires_two_arms_in_every_scene` | ① 6 处 committed 集中在 s1（A/B 各重复三次）⇒ P1 **False** 且 `per_scene={'s1': 6, 's2': 0, 's3': 0}` 如实打印；② 三场各 2/2 ⇒ P1 **True** 且 `missing` 为空；③ 4 场 8 处 ⇒ 总数达标也不判过（`missing` 指名「三场口径外的场 ['s4']」） |
| `test_p1_names_the_missing_arm` | 三场齐但 s3 缺 B 臂 ⇒ P1 False 且 `missing` 指名「s3 缺 B 臂」（旧口径只报 5/6≠6，说不出谁没交） |
| `test_report_contains_no_machine_absolute_paths` | 假 repo-root 在 tmp 下跑 `build_report`：报告 JSON 全文既无 `str(tmp_path)`、也无 `[A-Za-z]:[\\/]` 盘符形态；`k4_artifact` 相对仓库根；`generated_at` 带时区偏移 |
| `test_portable_path_outside_repo_has_no_drive_letter` | 仓外收据退化为 `<仓外>/文件名`，仍不内嵌盘符 |
| `test_report_out_path_lands_in_docs_not_repo_root` | 裸文件名 ⇒ `ROOT/docs/…`；带目录的相对路径 ⇒ `ROOT/…`；绝对路径照写 |
| `test_repo_root_default_is_script_derived` | `--repo-root` 缺省＝`ROOT`（且该根下真的含本脚本），不再写死主仓机器路径 |

既有 10 例口径**未放宽**：红线仍为 `k5_established` 恒 `False`、`mode=dry_run`、verdict 含「未通」（`test_dry_run_never_claims_established` 原样保留）；`n_criteria` 仍 10。

**验证状态（不装）**：

| 验收项 | 本 worker 会话结果 |
|---|---|
| `F:/Hermes/hermes-agent/venv/Scripts/python.exe -m pytest tests/test_k5_criteria_check.py -q` | **未自跑** |
| 核验器本体只读真跑（`--repo-root F:/agi/language-genome --out <临时文件>`） | **未自跑** |

原因：本 worker 会话的 Bash 只放通了 `ls`、`git status`、`git diff`、`python --version`
这类只读命令；`python -m pytest …`、`python -m py_compile …`、`python -c …`
以及核验器本体运行**全部触发权限提示且会话内无人批准**（上一轮即因此
exit 126）。故本批代码与回归**没有本地实跑证据**，通过与否须由主控在
验收门判定。

下面一行是**按代码口径的推演**（读 §1/§3 已记录的实跑证据 + 新校验逻辑推得，
本会话未能只读复核真收据，未自跑）：mc22_v2 收据 s1 两臂 committed、s2 两臂失败、
s3 无正文 ⇒ 新 P1 证据行应为 `committed=2/6（总数）` +
`per_scene={'s1': 2, 's2': 0, 's3': 0}`，判据终值不变（P1 仍 ❌、机械满足仍 2/10、
`k5_established=false`、exit 0）。

### 6.3 旧报告处置（交主控收口，本批未搬）

仓根的 `k5_report_20260925.json`（§0 那轮实跑产物，**内嵌 `F:\agi\language-genome\...` 绝对路径、旧 P1 总数口径**）本批未搬动：搬它要在 `docs/` 新建文件并删仓根的已跟踪文件，两者都不在本次允许改动白名单（脚本 + 其回归 + 本文档）内，按红线「范围外即停手上报」交主控一条命令收口：

```
git mv k5_report_20260925.json docs/k5_report_20260925.json
```

（或直接以新口径重跑 §6.1 命令生成 `docs/` 版本、再删仓根旧文件。）同批的仓根 `k4_accept_report_20260925.json` 属 K4 验收产物，不在本工具职责内，未动。

