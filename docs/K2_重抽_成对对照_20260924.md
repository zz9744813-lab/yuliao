# K2 重抽·成对对照真跑证据（2026-09-24，gate 解卡件）

- 生成器：`scripts/k2_pairs_gen.py`（op/标签/词表**单源继承**自
  `scripts/k2_contrast_extract.py` 的 `OPS` / `label_of` / `LABEL_STRATEGY`，
  不臆造）；本任务的全部工作在 worktree `feat/k2-pairs-regen`。
- writer 通道：OpenAI 兼容 `http://127.0.0.1:4000/v1`，模型 **mc22-flash**；
  密钥运行时从本地 secrets 文件读取（env `K2_PAIRS_WRITER_KEY` 可覆盖），
  **未写入任何交付文件/账本/缓存/证据**。
- 人类侧：真库 `segments` 表（LG_DATABASE_URL 指向
  `F:/agi/language-genome/data/language_genome.db`，43.8 万行）现成分段——
  筛选口径：`role≠benchmark`、长度窗 [80,350] 字、六门信号词
  （解释/心理/节拍/修饰 + 门1 双侧词表）**全零命中**（无信号基线）、
  scene_keys 机械派生（高频 2~3 字连续汉字串）；确定性 (work_id, ordinal)
  序、扫描上限 20,000/op。**不编造人类文本**。

## 1. 实际命令（原样可重跑）

```
LG_DATABASE_URL=sqlite:///F:/agi/language-genome/data/language_genome.db \
F:/Hermes/hermes-agent/venv/Scripts/python.exe scripts/k2_pairs_gen.py \
  --n-per-op 12 --on expression_strategies_v2 \
  --writer-base http://127.0.0.1:4000/v1 --writer-model mc22-flash \
  --out F:/agi/language-genome/_pairs/k2_pairs_20260924.json \
  --ledger F:/Hermes/team/judge/k2pairs_ledger.jsonl

# 抽取器门检（只出统计不落库；--live 未运行——fail-closed 双闸原样保留）
LG_DATABASE_URL=sqlite:///F:/agi/language-genome/data/language_genome.db \
F:/Hermes/hermes-agent/venv/Scripts/python.exe scripts/k2_contrast_extract.py \
  --dry-run --pairs-file F:/agi/language-genome/_pairs/k2_pairs_20260924.json
```

## 2. 生成对数与幂等实证（实际输出）

- 对文件：`F:/agi/language-genome/_pairs/k2_pairs_20260924.json` ——
  **48 对**（4 op × 12），逐对带 op / strategy_key（由 `label_of(op)` 唯一
  决定）/ human_text（含 span 0..len、sha256、segment_id、text_version）/
  ai_text / scene_keys / meta（work_id、writer_model）。
- 旁路账本：`F:/Hermes/team/judge/k2pairs_ledger.jsonl` —— **96 条
  writer_call + 48 条 cache_hit**：第一轮 48 调用（09:2x，psych 指令未含
  门1特征词句式）→ 指令修正后第二轮 48 调用（09:3x，即对文件现内容）→
  第三遍幂等核验 **48 条 cache_hit、0 次调用**（生成器重跑不烧 writer）。
- 拒覆盖守卫生效实证：第二轮初跑因 `--out 已存在` 被拒（退出非零），
  清理首轮对文件后才放行——对文件不静默覆盖。
- **缓存键含指令哈希**（首轮后修复的幂等漏洞）：改指令不换键会静默复用
  旧输出——修正后指令变 ⇒ 键变 ⇒ 必重生成，续跑对同指令才命中。

## 3. 抽取器 --dry-run 逐 op 通过/拒绝统计（第二轮 48 对，实际输出）

| op | 通过 | 拒绝 |
|---|---|---|
| add_interpretation | 9 | 3 |
| add_psych_narration | 10 | 2 |
| split_beats | 6 | 6 |
| dilute_modifiers | 10 | 2 |
| **合计** | **35** | **13** |

拒绝理由分类（reason_classes，一条对可计多类）：长度比超界 ×5（split_beats
改写长度越出 [1.2, 6.0]）、混合操作 ×3（模型违禁词渗入——指令明禁但
mc22-flash 未完全服从，如实拒）、场景指称交集为空 ×2、cross-strategy
互斥 ×2、add_interpretation 构造不符 ×2（新增解释句未锚定）、
dilute_modifiers 构造不符 ×1。

两轮对比（指令修正的效果，均如实记录）：第一轮 31/48（psych 仅 3/12——
指令禁解释词时模型连「心里明白」这类**门1特征词**也一并回避，致
「S1 双向未命中」×9）；修正指令明确「至少一句用『她心里明白……』句式」
后 psych 3→10，总通过 31→35。门 0/4/5 全程未放宽。

## 4. 离线回归（验收命令，实跑）

```
F:/Hermes/hermes-agent/venv/Scripts/python.exe -m pytest \
  tests/test_k2_contrast_extract.py tests/test_k2_pairs_gen.py -q
→ 32 passed（含抽取器既有 1 例 xfail 残余钉子），exit 0
```

test_k2_pairs_gen.py 五钉：① schema 合法且 op 全覆盖（build_pairs 直接
可吃）；② 标签单源（HUMAN_FORBIDDEN=六词表并集、label_of 漂移即红、
非法 op 抛错）；③ 缓存/续跑幂等（重跑 0 次调用、产物逐字相同）；
④ happy path 过抽取器**门0**（本夹具按全六道门构造，实际断言 gate_pair
全过——更强口径）；⑤ writer 不可达 fail-closed（网络异常/HTTP 500/
空 content/截断 finish=length 各报各错；主流程抛出后不写对文件、不伪造）。

## 5. 纪律声明

- 门 0/4/5 未放宽未绕过；`label_of` 语义未动；`--live` 未运行
  （fail-closed 双闸原样保留，K2-CARDS 落库时机由派工方另定）；
- 未动任何既有表结构（对真库只 SELECT）；
- 密钥不入库不入交付物；writer 全程可用（本轮无故障停机事件）；
- 第一轮（31/48）与第二轮（35/48）均入档，无「预计会通过」式结论。
