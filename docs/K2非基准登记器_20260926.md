# K2 非基准供给登记器（k2-rereview-nonbench，2026-09-26）

- 件：`scripts/k2_nonbench_register.py`（登记器 + `--preflight` 前后对照）
- 回归：`tests/test_k2_nonbench_register.py`（17 用例，全离线合成库）
- 基线：分支 `task/k2-rereview-nonbench` @ `bd256c1`（不 commit/merge/push，
  交付物以工作树文件形式存在）

## 0. 本件回答什么问题（及本会话的执行边界——先说黑纸白字）

真库（`F:/agi/language-genome/data/language_genome.db`）里非基准证据供给为
零：`work_sources` 没有任何 `production_nonbenchmark_` 前缀族行，全部既有
`strategy_instances` 证据几乎压在 `role=benchmark` 段上 ⇒ K3 的 A 臂
（非基准独立证据臂，`app/knowledge_query._evidence_for` 的后三个来源闸过滤
后的区间数）恒空。机制侧（`--source-scope nonbenchmark`、来源闸镜像、
服务端并集封底）已在前作修完；**缺的是「真实登记动作 + 登记前后对照的
机器证明」**，即本件。

**执行边界（诚实声明）**：本会话中所有解释器执行（venv python、pytest、
git 写以外的一切命令）均被权限门拒绝——**收口判据
`pytest tests/test_k2_nonbench_register.py -q` 的 rc=0 本会话未能亲跑，
下文所有「预期」均标注为未自跑**；真库盘查数字一律引用主控只读实测与前作
`docs/K5非基准供给缺口_20260926.md` 的落库读数（标注出处），本件**未自跑
任何真库读数**。文档中出现的每一处数字都带「本次实跑 / 前作实测(引用) /
未自跑」标签，无一裸数字。

## 1. 设计（判据零新建，全部单源复用）

| 判据 | 唯一来源（import，不复制） |
|---|---|
| 来源类型合规 | `k2_extract_backfill.nonbenchmark_compliant_source` + 前缀常量 `NONBENCHMARK_SOURCE_TYPE_PREFIX` |
| 段 role 口径 | `k2_extract_backfill._role_passes_scope("nonbenchmark", role)` / `BENCHMARK_ROLE` |
| 可达段闸 | `k2_extract_backfill._k3_source_gate`（= `_evidence_for` 来源闸同序镜像） |
| A 臂证据数 | `app.knowledge_query._evidence_for` 本尊 |
| status 门诚实注记 | `KQ.eligible_statuses(version)` / `KQ.ELIGIBLE_OBSERVATION` |
| 列宽硬校验 | `register_work_sources._check_source_type_width` |
| 探针复审标记 | `app.knowledge_extract.REVIEW_MARKER_NEW_DEF`（不新造标记位） |

管道：`--corpus`（.txt 文件或目录）+ `--tag` ⇒ source_type 拼为
`production_nonbenchmark_<tag>`（拼完仍过唯一合规入口，拼不合即拒，C2）；
段落=空行分界确定性切分；`work_id = "WK-" + sha256(tag|文件名)[:12]`
（**不含内容**——内容漂移必撞 drift_conflict，不静默重锚，K1-A 纪律）；
`--commit` 单事务（`BEGIN IMMEDIATE`，异常整体 ROLLBACK）只 INSERT
works/work_sources/segments 三表增量行（C3：无 UPDATE/DELETE 路径，回归
源码 grep 钉死）；登记的段 `integrity` 留 NULL——本件**不自证 `src_ok`**
（那是 `scripts/source_check.py` 的活），故这些段进 K3 来源闸口径的可达
统计、但**不进 K2 抽取池**（残留 §4）。

`--preflight`（C4）：
1. 真库 `mode=ro` 算「登记前」`k3_reachable_segments` /
   `a_arm_evidence_count`；
2. sqlite backup API 把库复制进 tempfile 一次性合成副本（真库连写意图都
   不存在），在副本上走**与 --commit 同一套**写路径 + 注入预演探针实例
   （`extractor_model="preflight-probe"`、status=verified、新口径复审标记，
   每条既有策略至多 1 条，`--probe-max` 封顶默认 50）；
3. 副本上算「登记后（合成库模拟）」两指标并打印对照行；登记后任一为 0 ⇒
   rc=2 NO-GO（可证伪方向：机制若回退，preflight 必须炸）。

预期真库读数（**未自跑**，理由见 §0；结构上等价的前作实测见 §2）：
登记前 `a_arm_evidence_count=0`；登记后（合成库模拟）两指标 >0 且
`n_k3_status_gate_pass_strategies=0`——最后这个 0 是本件刻意打印的防误读
项：**「A 臂在合成库上非空」≠「K4 已解冻」**。真库全部策略卡仍停在
status=hypothesis，K3 候选预筛（status/observation 门）通过数为 0，登记
不改变它；解冻与否仍是策略审查席逐级晋升（`k5_promotion_write.py`）的活。

## 2. 依据的真库底数（全部为引用，非本次自跑）

- 主控只读实测（本任务书背景，可复跑 `mode=ro`）：`work_sources.source_type`
  = fixture 2 / human_fiction 7，`production_nonbenchmark_*` **0 行**；
  `segments.role` = benchmark 833 / NULL 437,585；strategy_instances 83 条
  中 82 条压在 benchmark 段 ⇒ 合格非基准证据 0。〔标签：未自跑（引用主控）〕
- 前作 `docs/K5非基准供给缺口_20260926.md` 落库读数：`src_ok` true 4,872 /
  false 1,919 / 未校验 431,627（98.45%）；非基准合格段上界 392,772、
  过 src_ok 口径 4,213；覆汉《覆汉（榴弹怕水）》`WK-dc90993434e9`
  human_fiction / corpus-v1 共 35,974 段，src_ok 未校验 100%，K3 来源闸
  过闸但无一条已核证据可用。〔标签：前作实测(引用)，本次未自跑〕
- 本件回归：全离线合成库，**不产生任何真库读数**。〔标签：本次实跑=否，
  套件事件本身亦未自跑，见 §0〕

## 3. 反向验证（承重的变异清单）——本会话未自跑，命令原样可抄

**声明：以下 M1–M4 在本会话均未执行**（解释器被权限门拒绝，§0）。每条给出
变异 → 预期炸哪条用例 → 精确恢复 → 校验 → 复绿命令；恢复校验用文件哈希：
`certutil -hashfile scripts\k2_nonbench_register.py SHA256`（或
`git diff -- scripts/k2_nonbench_register.py` 为空即恢复到位）。
复跑入口（收口判据，**预期 rc=0，未自跑**）：

```
F:/Hermes/hermes-agent/venv/Scripts/python.exe -m pytest tests/test_k2_nonbench_register.py -q
```

- **M1（承重：C1 零写入）**：把 `build_report` 里 dry-run 分支的
  `compute_metrics(db_path)` 之后补一行 `commit_registration(db_path, plan)`
  （即「dry-run 顺手真写」的堕落形态）。预期红：
  `test_dry_run_is_default_and_writes_nothing`（md5/mtime 变 + `open_write_connection`
  换炸替身被触发）、`test_recommit_is_idempotent_and_opens_no_connection`。
  〔预期，未自跑〕
- **M2（承重：C2 单源合规笔）**：在 `build_plan` 里把
  `k2b.nonbenchmark_compliant_source(source_type)` 换成
  `source_type.startswith("production_nonbenchmark_")` 的本地字面量判定
  （第二口径的典型堕落形态）。预期红：`test_source_type_is_prefix_family_and_single_sourced`
  （合规入口被换 False 后计划不再拒）。另 grep 断言类：任何在本文件新落
  前缀字面量的改动都会被该用例的「换入口即拒」结构抓住。〔预期，未自跑〕
- **M3（承重：C4 可证伪方向）**：把 `preflight` 的 go 判据
  `after["k3_reachable_segments"] > 0 and after["a_arm_evidence_count"] > 0`
  改成 `or`（放宽成「任一非空即放行」）。预期红：
  `test_preflight_refuses_when_after_stays_zero`（非法 text_version 场景下
  a_arm 同为 0 却因 reach>0…该场景两指标同 0，改判 `or` 后仍 NO-GO——
  因此**更强的变异**是直接把 `return report, EXIT_REFUSED, lines` 删掉，
  让 after=0 静默过；预期红同用例 rc 由 2 变 0）。〔预期，未自跑；两条
  子变异分别打「放宽」与「不炸」两个方向〕
- **M4（承重：C5 单事务）**：把 `commit_registration` 里
  `except Exception: con.execute("ROLLBACK"); raise` 的 ROLLBACK 行删掉
  （半写留库的堕落形态）。预期红：`test_rollback_when_midwrite_conflict`
  （三表行数前后不一致）。〔预期，未自跑〕

> M1–M4 任一条做完必须**精确恢复原文件并复跑全绿**再进入下一条；恢复以
> SHA256/`git diff` 空为准，不以「看起来改回去了」为准。

## 4. 残留（本件不回答的，逐条点名）

1. **收口判据未自跑**：pytest rc=0、§3 全部变异、以及任何真库 preflight
   实跑在本会话被权限门拒绝——三件交付物是**未执行态交付**，验收人第一步
   就是跑 §3 的复跑命令。〔标签：未自跑〕
2. **真库 `--commit` 未跑**：真语料（含覆汉这类 human_fiction 全量文本）
   尚未按本件登记；`production_nonbenchmark_*` 在真库里仍是 0 行（前作实测
   引用，§2）。登记动作本身留给主控批语料清单后执行。〔标签：未自跑〕
3. **src_ok 覆盖**：本件登记的段 integrity=NULL，**不进 K2 抽取池**；要把
   这些段变成可抽证据，还需 `source_check` 跑源检查 + K2 抽取回填 +
   `k2_rereview_queue` 复审链（各件职责不混）。〔标签：设计边界〕
4. **A 臂 ≠ 解冻**：真库 K3 status 门通过策略数仍 0（前作实测：8 卡全
   hypothesis）；「登记后 A 臂非空」只在合成库模拟意义下成立，不得引用为
   K4 解冻依据。〔标签：前作实测(引用)+设计边界〕
5. 作者/题材考据留空（author_id NULL、metadata_status=unverified）：登记
   ≠ 署名核对完成，K1-A 的「可考据才填」纪律原样保留。〔标签：设计边界〕

## 5. 回归用例 × 契约对照（17 条，全离线）

| 契约 | 用例 |
|---|---|
| C1 缺省零写入 / 判定不开可写连接 | `test_dry_run_is_default_and_writes_nothing`、`test_preflight_never_opens_real_write_connection` |
| C2 前缀族 + 显式 role | `test_source_type_is_prefix_family_and_single_sourced`、`test_role_benchmark_or_empty_refused` |
| C3 只增不改 | `test_registered_rows_are_additive_and_honest`、`test_no_update_delete_write_paths_in_source`、`test_benchmark_evidence_never_counts` |
| C4 前后对照 + 可证伪 | `test_preflight_contrast_before_zero_after_positive`、`test_preflight_refuses_when_after_stays_zero`、`test_preflight_note_is_honest_about_scope` |
| C5 单事务 + 幂等 + 漂移 | `test_commit_changes_exactly_the_new_rows`、`test_rollback_when_midwrite_conflict`、`test_recommit_is_idempotent_and_opens_no_connection`、`test_content_drift_refused_loudly` |
| C6 零模型调用 | `test_zero_model_calls_and_live_refused` |
| 单源纪律 / 不猜 | `test_metrics_route_through_single_source_gates`、`test_missing_db_reports_unreadable_not_guess`（bd256c1 同款：空 `--repo-root` + 顶掉 `DB_FALLBACK_ABS`，防主仓 cwd 假红） |

一句话交付：**登记器把「合规非基准来源入库」变成一条确定性、幂等、可预演
的管道；`--preflight` 用合成库模拟把「A 臂为什么是 0、登记后为什么能非 0」
打成两行机器可核的数字；而「A 臂非空 ≠ K4 解冻」被 `n_k3_status_gate_pass_strategies=0`
这行注记钉死在输出里。** 执行边界见 §0：全部运行类断言均未自跑，待验收
复跑。
