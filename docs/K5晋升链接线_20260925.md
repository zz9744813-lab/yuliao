# K5 晋升链接线：A 臂空的机械归因（2026-09-25，k4-unfreeze-preflight）

工作树：`F:/agi/_scratch/worktrees/k4-unfreeze-preflight`（分支
`task/k4-unfreeze-preflight`，基线 `b5c8685`）。只改三件：
`scripts/k5_criteria_check.py`、`tests/test_k5_criteria_check.py`、本文档。
纪律：真库 mode=ro 只读、零模型调用、未跑 --live、未 commit/merge/push
（留主控独立验收）。

## 1. 晋升链缺口（真跑输出，机械判定、不许猜）

核验器新增 `promotion_gap_preflight` section——K5 报 C3/P2
「n_techniques≥1 场数=0」时，机械回答「为什么是 0」：

```
F:/Hermes/hermes-agent/venv/Scripts/python.exe scripts/k5_criteria_check.py \
  --repo-root F:/agi/language-genome
→ exit 0；报告新增 promotion_gap_preflight section（其余字段逐字不变）
```

真跑实际输出（四项）：

| 项 | 实测 |
|---|---|
| (1) 真计数 | knowledge_packages=**0**；strategy_conditions=**0** |
| (2) 逐卡（8 张 legacy 卡） | 全部 status=**hypothesis**、observation=**observed**、scope=UNCERTAIN；**k3_eligible 全部 False**（verified+observed 才可服务——资格集合单源 import `app.knowledge_query`，不本地重定义） |
| (3) 三席 verdict 一致性 | 8 卡有评审（各 3 席），**完全一致卡数 0/8**——每卡的席位档位均为混判（retire/merge/rewrite 两至三档并存），无一同判 verified |
| (4) 机械结论 | **『卡未晋升』**：K3 可服务卡数=0（status∈{verified} 且 observation∈{observed,replicated} 的卡为零）——A 臂空与 K3 过滤无关，缺口在晋升链；席位一致性 0/8 与 docs/策略语义审查_判定模型版_20260923.md 的 24 席判定（0 条 verified）互证 |

判定矩阵（写进代码，四态机械分流，无猜测分支）：
- 库不可读 → `insufficient_evidence`（不许猜）；
- 收据 A 臂非空 → `not_applicable`（非缺口态不硬套结论）；
- 可服务卡数=0 → **『卡未晋升』**（缺口在晋升链：语义审查/两席 PASS/
  改 status 均未发生）；
- 可服务卡数>0 而 A 臂仍空 → **『K3 过滤』**（缺口在查询侧
  scope/条件/版本/证据实例，逐卡 scope 附证据供分诊）。

当前真库落在第三态——**「卡未晋升」**。这意味着解卡路径不变：
合并卡 S1/S2（docs/策略卡合并方案_20260923.md）→ 成对对照证据
（48 对已生成、35/48 过六门）→ 两席独立判定 → status 晋升 → K3 自然
可服务；**不需要**动 K3 过滤逻辑。

## 2. 三席一致（实测口径）

- 一致定义（机械）：该卡评审数 ≥2 且 verdict 档位唯一。
- 实测 **0/8 完全一致**：8 卡均为 3 席混判（如「保留原句的递进铺陈」
  = retire/merge/rewrite 三档各一）。与 docs/策略语义审查_判定模型版
  _20260923.md「多席完全一致（判定档位）的卡：0/8」一致——判定层
  连档位都未收敛，晋升链的「两席 PASS」步骤更未发生。
- 该指标入报告 `(3)_seats.verdict_fully_consistent_cards`，逐卡明细在
  `(2)_cards[].verdicts / verdict_consistent / judge_kinds`。

## 3. 只读纪律（实现层）

- 真库打开一律 `sqlite3.connect("file:...?mode=ro", uri=True)`；库文件
  缺失或打开失败 → `db_unavailable=true` + `insufficient_evidence`，
  **不造半套数据**（`(1)_counts` 等键不出现在缺库报告里）；
- 零模型调用：纯 SELECT 四张表（expression_strategies_v2 /
  strategy_reviews / knowledge_packages / strategy_conditions）；
- 未跑 --live：未跑，任务书禁。

## 4. 默认语义零变化

- **无任何新增 CLI 参数**：预检随缺省报告自动并入（不加参数时报告除
  新增 `promotion_gap_preflight` section 外逐字不变）；
- tests 钉死：`test_report_adds_only_new_section_by_default`——报告
  顶层键集 = 旧九键 + 新一键，criteria/verdict/k5_established 口径原样；
  旧 16 例测试全绿（未改任何断言）即行为不变的另一半证明；
- `check_criteria` / `ten_scene_dry_run` / CLI 参数与落盘路径均未动。

## 5. 未自跑清单

- **live 三场/十场复跑**：未自跑——零配额纪律（任务书禁 --live、禁真实
  模型调用）；晋升链走完后由派工方另行授权。
- **对合并卡 S1/S2（v2:留白摊开/v2:节拍注水）的预检**：未自跑——真库
  v2 卡尚未落库（合并卡未执行 status 晋升流程），当前报告只覆盖已落库
  的 8 张 legacy 卡；S1/S2 落库后预检自动覆盖（同一 SELECT）。
- **K3 过滤态的实测**：未自跑——当前真库无可服务卡，落在「卡未晋升」
  态；「K3 过滤」判定分支由离线假库测试钉死（假库含 verified+observed
  卡 + A 臂空收据 → 判 K3 过滤），真库到达该态后自然实测。

## 6. 验收

```
F:/Hermes/hermes-agent/venv/Scripts/python.exe \
  -m pytest tests/test_k5_criteria_check.py -q
→ 21 passed，exit 0
```

新增 5 例：卡未晋升（三席一致 1/2 卡如实）/ K3 过滤（有可服务卡而
A 臂空）/ A 臂非空 not_applicable / 库不可读不猜（缺键不造半套）/
默认零变化（键集=旧+新，缺库态诚实）。
