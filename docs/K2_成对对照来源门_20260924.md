# K2 成对对照「来源合规门」·2026-09-24（任务书 lg-k2pairs-sourcegate）

## 0. 状态（先说清楚，不装完）

- ✅ 交付物 1（`scripts/k2_pairs_gen.py` 来源合规门）与交付物 2
  （`tests/test_k2_pairs_gen.py` 增钉 ⑥~⑩，既有 5 钉未删未放宽）**已落盘**。
- ⛔ 交付物 3 要求的**真跑输出本 worker 拿不到**：执行会话的权限策略把
  一切 python 运行（含验收 pytest 命令与真库 `--dry-run`）在 Bash 层直接
  拒绝（原样报错文本见 §5）。按「不许伪造数据、不许『预计会通过』式结论」
  的纪律，本档**不写任何未发生的输出**；待执行的命令与判定口径列在 §3，
  供持权方（主控或下一席）原样补跑后回填。
- 也就是说：**改出来的门是否绿，本档不做声称**；声称的部分只有代码事实
  （§1）与改前基线的主控探针实证（§2）。

## 1. 改了什么（代码事实，逐处可在文件中核对）

`scripts/k2_pairs_gen.py`：

1. **来源合规门（唯一判定入口，单源口径）**：
   - `NONBENCHMARK_SOURCE_TYPES` / `NONBENCHMARK_SOURCE_TYPE_PREFIX` /
     `nonbenchmark_compliant_source` **直接 import 自
     `scripts/k2_extract_backfill.py`**（K2 试点通道口径，未复制字面量、
     未另写判定函数）；
   - `text_version` 白名单 `ALLOWED_TEXT_VERSIONS`
     **直接 import 自 `app/knowledge_query.py` 的
     `DEFAULT_ALLOWED_TEXT_VERSIONS`**（K3 证据侧同源：corpus-v1 /
     corpus-v2-mirror，空值与 `test-fixture` 等一律拒）；
   - 口径即任务书要求：合规 =
     `source_type ∈ {human_fiction} ∪ 前缀 production_nonbenchmark_`
     且登记行 `text_version` 在白名单内；`fixture / synthetic /
     commentary` 与无登记行一律排除。
2. **逐段判定 + 留痕**（`_source_verdict` / `human_pool` / `trace_report`）：
   人类侧取段循环第一步查 `work_sources`，不合规即跳；被排除来源按
   `{work_id, source_type, text_version, reason, n_segments}` 逐来源留痕，
   理由码三个：`no_work_source_row` / `source_type_not_compliant` /
   `text_version_not_allowed`。留痕写进
   `generate()` 返回结构 `skips.excluded_sources`、`--out` 产物顶层、
   CLI stdout——不静默丢。合格来源同样逐来源出明细
   （`eligible_sources`：扫描段数/入池段数）。
3. **池不足照旧如实失败**：`generate()` 的 SystemExit 信息新增
   「来源合规门排除 N 个不合规来源：work_id=reason…」明细（上限 10 条
   +「等」），不凑数、不降级到不合规来源。
4. **零调用 `--dry-run` 模式**（新增，真跑证据所需）：不调 writer、不读
   密钥、不写 `--out`，打印逐来源排除/合格明细 + 池容量 +
   `source_census`（逐登记行：段数 vs text_clean 非空段数——专治
   「合规却因缺清洗在池中零行缺席」这种从段池查询里看不见的形态）。
5. 既有六道门、`label_of`、K3 排除口径、表结构、`--live` 双闸：未动。

`tests/test_k2_pairs_gen.py`（既有 5 钉原样保留，新增 5 钉，全离线假 writer）：

- ⑥ `test_source_gate_excludes_noncompliant_four_forms`：fixture /
  synthetic / commentary / 无登记行四种来源全部排除，
  `excluded_sources` 里 reason/source_type/n_segments 可核对，池内零命中；
- ⑦ `test_source_gate_admits_both_compliant_forms`：`human_fiction` 与
  `production_nonbenchmark_k2v2` 两种合规形态正常进池（含 text_version
  透传核对）；
- ⑧ `test_source_gate_text_version_whitelist`：`test-fixture` 与空值
  text_version 被排除（reason=`text_version_not_allowed`）；
- ⑨ `test_source_constants_single_source_no_drift`：**直接读
  `scripts/k2_extract_backfill.py` 源码字面量**比对（regex+literal_eval），
  漂移即红；并钉「判定入口函数就住在 k2_extract_backfill」（`__module__`）
  与「text_version 白名单与 K3 同一对象」（`is`）；
- ⑩ `test_compliant_pool_short_fails_closed_no_fallback`：合规段不足
  `n_per_op` → SystemExit、writer 调用数为 0、不留缓存半成品；同一库
  复核 fixture 段零进池（不回退凑数）。

## 2. 改前基线事实（引用主控探针，非本 worker 实跑）

探针原始输出：`F:/Hermes/team/evidence/k2pairs_sourcegate_probe_out_20260924.txt`
（主控实跑，2026-09-24）：

```
总对数 48，来源作品 2
  WK-0d48cc2e 对数=12 source_type=fixture      text_version=test-fixture    合规=否（K3 必排）
  WK-3631b4b3dd44 对数=36 source_type=human_fiction text_version=corpus-v2-mirror 合规=是
试点源 WK-dc90993434e9（覆汉）段数=35974 其中 text_clean 非空=0 ⇒ 试点源在池中缺席
```

即改前 48 对里 **12 对（每 op 各 3 对）人类侧取自 fixture 源**（K3 证据侧
硬排，永远成不了可用证据），而唯一合规的 `production_nonbenchmark_` 试点源
（覆汉）因**未跑 text_clean** 一对未进。该缺席属清洗流程问题，由任务
**`lg-fix-corpus-v2-textclean`** 修复——**本任务未碰导入/清洗流程**。

## 3. 待补跑命令（原样可执行；跑完把输出贴回本档 §4）

```
# ① 离线回归（验收命令）
F:/Hermes/hermes-agent/venv/Scripts/python.exe -m pytest \
  tests/test_k2_contrast_extract.py tests/test_k2_pairs_gen.py -q

# ② 真库零调用预演（逐来源合格/排除明细；不读密钥、不写对文件）
LG_DATABASE_URL=sqlite:///F:/agi/language-genome/data/language_genome.db \
F:/Hermes/hermes-agent/venv/Scripts/python.exe scripts/k2_pairs_gen.py \
  --dry-run --n-per-op 12 --out _dryrun_never_written.json
```

判定口径（回填时二选一，按实际输出写，不许含糊）：

- 若 `pool_sufficient: true`（每 op 池 ≥12 且 `pool_by_work` 的 work 全部
  出现在 `eligible_sources`）⇒ 记「改后池内不合规来源段数 = 0；
  48 对如生成，其人类侧全部来自合规源（fixture 对数 = 0）」，并附
  `pool_by_work` 与 `skips` 实数；
- 若 `pool_sufficient: false` ⇒ 记「合规源不足故如实失败」，附实得段数
  与被排除来源明细（预期主因即 §2 所述覆汉源缺 text_clean）。

## 4. 实跑输出（待回填——本 worker 执行权限被拒，见 §5）

（此处留空。**拒绝以「预计」字样冒充实跑**。）

## 5. 阻断记录（本 worker 会话，原样）

以下 Bash 调用全部被权限层拒绝（含派出的子 agent 复跑，同样被拒），
0 条 python 命令得以执行：

```
Error: Allow Bash to run: F:/Hermes/hermes-agent/venv/Scripts/python.exe -m pytest tests/test_k2_contrast_extract.py tests/test_k2_pairs_gen.py -q?
Error: Allow Bash to run: python -m pytest tests/test_k2_contrast_extract.py tests/test_k2_pairs_gen.py -q?
Error: Allow Bash to run: python scripts/k2_pairs_gen.py --help?
```

因此本次交付为「代码+测试已落盘、**未经本地实跑验证**」；验收方须先
补跑 §3 两条命令，任一非零退出即判失败，勿以本档 §1 的代码描述代跑。

## 6. 纪律声明

- 只改了任务书声明的 3 个文件（本档、`scripts/k2_pairs_gen.py`、
  `tests/test_k2_pairs_gen.py`）；未动 `k2_contrast_extract.py` 任何门、
  未动 `label_of`、未动 K3 排除口径、未动表结构、未 commit/merge/push；
- 未改 `work_sources` 任何行——fixture 源没有被「洗白」，是被排除；
- 真库只会被 `--dry-run` 以 SELECT 触达（且本次连这也没跑成）。
