# K5 供给口径真计数（A2 整改，2026-09-25）

- 工具：`scripts/k5_supply_recount.py`（`sqlite3` `mode=ro` 只读、零写库、零模型调用；不改任何 `status`/`scope`，不做解冻/冻结裁定）。
- 库：`F:/agi/language-genome/data/language_genome.db`｜SQLite 3.53.1｜运行时间 2026-09-25 10:14:27Z。
- 达标判据（复核者指定）：≥2 作品在口径 A 下各自 ≥10,000 段。

## 0. 实跑命令

```
F:/Hermes/hermes-agent/venv/Scripts/python.exe scripts/k5_supply_recount.py
→ exit 0（stdout 与本文件同口径逐作品输出，可复算）
F:/Hermes/hermes-agent/venv/Scripts/python.exe -m pytest tests/test_k5_supply_recount.py -q
→ exit 0（临时夹具库，不依赖真库）
```

## 1. src_ok 覆盖口径（键存在性五分类，全库）

依据 `app/engine.py` `_integrity_state`：只 `is True` 计完好、`is False` 计判坏、其余计**未校验**。

| 状态 | 含义 | 段数 |
|---|---|---:|
| `true` | 完好（src_ok=true） | 4,872 |
| `false` | 判坏（src_ok=false） | 1,919 |
| `loose` | 键在但类型不严（数字/字符串/null）→未校验 | 0 |
| `missing` | 合法 JSON 但缺 src_ok 键→未校验 | 431,617 |
| `nojson` | integrity 为 NULL/非法 JSON→未校验 | 10 |
| 合计「未校验」 | missing+nojson（+loose 单列） | 431,627 |

**`src_ok` 只在 6,791 段上被真实计算过；其余 431,627 段缺该键——属「未校验」，不是「判坏」**（loose 0 段另列，同样不得当作已校验）。

## 2. 三口径逐作品真计数（原样输出，非结论摘录）

### 2.1 口径 A：`json_extract(integrity,'$.src_ok') IS TRUE` AND `role≠'benchmark'` AND `json_array_length(allowed_purposes)>0`（复核者指定分母）

```sql
SELECT work_id, COUNT(*) FROM segments WHERE json_valid(segments.integrity)=1 AND json_extract(segments.integrity,'$.src_ok') IS TRUE AND (segments.role IS NULL OR segments.role<>'benchmark') AND EXISTS (SELECT 1 FROM work_sources ws WHERE ws.work_id=segments.work_id AND (CASE
  WHEN ws.allowed_purposes IS NULL OR json_valid(ws.allowed_purposes)=0 THEN 0
  WHEN json_type(ws.allowed_purposes)<>'array' THEN 0
  ELSE json_array_length(ws.allowed_purposes) END)>0)
GROUP BY work_id;
```

| work_id | 作品 | 段数 |
|---|---|---:|
| WK-8e8e0459284d | 斗罗大陆（唐家三少） | 876 |
| WK-6e5d2623 | 琼明神女录（精校） | 840 |
| WK-3631b4b3dd44 | 斗罗大陆（唐家三少）（corpus v2） | 828 |
| WK-a052258c | 将夜（猫腻） | 656 |
| WK-d999c2c8ea26 | 将夜（猫腻）（corpus v2） | 623 |
| WK-6c5ea9081547 | 凡人修仙传（忘语） | 390 |
| WK-648c2920 | fixture_chen_clan | 4 |
| WK-0d48cc2e | fixture_wuguan | 3 |

- **合计 4,220 段｜最大单作品 876 段｜≥10,000 段的作品数 0**

### 2.2 口径 A 严格布尔补列：同上但 `json_type(...,'$.src_ok')='true'`（排除数字 1/字符串 "true" 等非布尔掺水）

```sql
SELECT work_id, COUNT(*) FROM segments WHERE json_valid(segments.integrity)=1 AND json_type(segments.integrity,'$.src_ok')='true' AND (segments.role IS NULL OR segments.role<>'benchmark') AND EXISTS (SELECT 1 FROM work_sources ws WHERE ws.work_id=segments.work_id AND (CASE
  WHEN ws.allowed_purposes IS NULL OR json_valid(ws.allowed_purposes)=0 THEN 0
  WHEN json_type(ws.allowed_purposes)<>'array' THEN 0
  ELSE json_array_length(ws.allowed_purposes) END)>0)
GROUP BY work_id;
```

| work_id | 作品 | 段数 |
|---|---|---:|
| WK-8e8e0459284d | 斗罗大陆（唐家三少） | 876 |
| WK-6e5d2623 | 琼明神女录（精校） | 840 |
| WK-3631b4b3dd44 | 斗罗大陆（唐家三少）（corpus v2） | 828 |
| WK-a052258c | 将夜（猫腻） | 656 |
| WK-d999c2c8ea26 | 将夜（猫腻）（corpus v2） | 623 |
| WK-6c5ea9081547 | 凡人修仙传（忘语） | 390 |
| WK-648c2920 | fixture_chen_clan | 4 |
| WK-0d48cc2e | fixture_wuguan | 3 |

- **合计 4,220 段｜最大单作品 876 段｜≥10,000 段的作品数 0**

### 2.3 口径 B：`role≠'benchmark'` AND `json_array_length(allowed_purposes)>0`（不看 src_ok）

```sql
SELECT work_id, COUNT(*) FROM segments WHERE (segments.role IS NULL OR segments.role<>'benchmark') AND EXISTS (SELECT 1 FROM work_sources ws WHERE ws.work_id=segments.work_id AND (CASE
  WHEN ws.allowed_purposes IS NULL OR json_valid(ws.allowed_purposes)=0 THEN 0
  WHEN json_type(ws.allowed_purposes)<>'array' THEN 0
  ELSE json_array_length(ws.allowed_purposes) END)>0)
GROUP BY work_id;
```

| work_id | 作品 | 段数 |
|---|---|---:|
| WK-6c5ea9081547 | 凡人修仙传（忘语） | 154,674 |
| WK-d999c2c8ea26 | 将夜（猫腻）（corpus v2） | 68,626 |
| WK-a052258c | 将夜（猫腻） | 68,481 |
| WK-3631b4b3dd44 | 斗罗大陆（唐家三少）（corpus v2） | 37,863 |
| WK-8e8e0459284d | 斗罗大陆（唐家三少） | 37,722 |
| WK-6e5d2623 | 琼明神女录（精校） | 34,228 |
| WK-648c2920 | fixture_chen_clan | 10 |
| WK-0d48cc2e | fixture_wuguan | 7 |

- **合计 401,611 段｜最大单作品 154,674 段｜≥10,000 段的作品数 6**

### 2.4 口径 C：全部段按作品（无任何过滤）

```sql
SELECT work_id, COUNT(*) FROM segments
GROUP BY work_id;
```

| work_id | 作品 | 段数 |
|---|---|---:|
| WK-6c5ea9081547 | 凡人修仙传（忘语） | 154,809 |
| WK-a052258c | 将夜（猫腻） | 68,626 |
| WK-d999c2c8ea26 | 将夜（猫腻）（corpus v2） | 68,626 |
| WK-3631b4b3dd44 | 斗罗大陆（唐家三少）（corpus v2） | 37,863 |
| WK-8e8e0459284d | 斗罗大陆（唐家三少） | 37,863 |
| WK-dc90993434e9 | 覆汉（榴弹怕水） | 35,974 |
| WK-6e5d2623 | 琼明神女录（精校） | 34,640 |
| WK-648c2920 | fixture_chen_clan | 10 |
| WK-0d48cc2e | fixture_wuguan | 7 |

- **合计 438,418 段｜最大单作品 154,809 段｜≥10,000 段的作品数 7**

补列读数：IS TRUE 口径合计 4,220 − 严格布尔 4,220 = 0 段（两口径一致，无非布尔掺水）。

## 3. 与主控 U4 实测（任务书背景）对照

| 项目 | 主控 U4 数字 | 本脚本复算 | 是否一致 |
|---|---:|---:|---|
| 全部段 | 438,418 | 438,418 | 一致 |
| integrity.src_ok 键存在 | 6,791 | 6,791 | 一致 |
| src_ok 值为 true | 4,872 | 4,872 | 一致 |
| src_ok 值为 false | 1,919 | 1,919 | 一致 |
| 缺 src_ok 键（未校验） | 431,627 | 431,627 | 一致 |
| 口径 A 合计 | 4,220 | 4,220 | 一致 |
| 口径 A 最大单作品 | 876 | 876 | 一致 |
| 口径 B 合计 | 401,601 | 401,611 | **不一致** |
| 口径 B 最大单作品 | 154,674 | 154,674 | 一致 |

## 4. 与 docs/K5判据核验_20260925.md 的供给数字一致性核对

扫描对象：`F:\agi\_scratch\worktrees\k5-supply-recount\docs\K5判据核验_20260925.md`；扫描规则：含「\d+ 段」或「供给」的行。

**扫描结果：原文未出现任何语料供给（段数）数字**——`docs/K5判据核验_20260925.md` 中的数字全部是 token/调用/止损口径（如 tokens=2,167、12,810、止损 800 万），不构成供给声明，无需（也无从）与本报告做供给数字对照；如实记录为「无供给数字可对照」，不替原文虚构对照项。

## 5. 口径结论（只写事实判断，不裁定解冻）

1. 按复核者指定的口径 A：合计 4,220 段，最大单作品 876 段，≥10,000 段的作品数 0。「≥2 作品各自 ≥10,000 段」**不成立**。故复核报告 §3.3 拟改写的「供给充足」在当前口径下不成立，「供给不足」也不应凭旧数字沿用——以本表可复算口径为准。
2. 覆盖缺口：`src_ok` 仅 6,791 段被真实计算，431,627 段缺该键（未校验，非判坏），见 §1。
3. 口径 A 达标的最小动作（**只列动作与代价估计，是否执行由主控裁定**）：
   - 对口径 B 中未校验段（缺键/非法 JSON/类型不严）补跑 `scripts/source_check.py`（每段一次 LLM 判定写入真布尔 `src_ok`；现有 true/false 段按幂等口径跳过）——**约 395,648 段待补跑**；
   - 补跑后口径 A 逐作品段数的算术上限 = 口径 B 逐作品段数（≥10,000 段作品数 6）：
     口径 B 已有 6 个作品 ≥10,000 段，达标在算术上可能，实际取决于补跑通过率（false 段不可翻正）。
   - 或者：与主控重议分母（口径 A 系复核者指定，本文不改判据、只如实报数）。

## 6. 红线声明

- 本报告由只读复算产生：未写库、未改 `expression_strategies_v2.status`/`scope`、零模型调用；不构成解冻/冻结裁定。
- 「≥2 作品各≥1 万段」判据出自对抗性复核 §3.2-A2，本文只复算其当前读数。

