# 8 条 legacy 策略 status/scope 拍板简报（一页式 · 只读 · 零写库 · 零模型调用）

> 任务号：lg-brief-strategy-scope-decision ｜ 基线：worktree `F:/agi/_scratch/worktrees/strategy-brief` @ `e8fbccd`（= main HEAD）
> 裁定权：**集霸（朱十一）**。本简报只列现状、选项与代价，**结论留空**（见文末勾选区）。
> 本简报不产生任何真值改动：8 条策略的 `status`/`scope`/`scope_ids`/`scope_basis` 与 `knowledge_packages` 全程零改动。

## 0. 数据来源与实查口径（先读这段）

所有数字来自**真库只读实查**：`scripts/k2_unlock_sim.py` 于
`F:/agi/language-genome/data/language_genome.db` 上的完整跑（DB-API URI `file:…?mode=ro`、每条连接 `PRAGMA query_only=1` 回读自证；**实跑时间戳 2026-09-25T05:35:04+00:00**，exit 0，S0 自校验通过＝逐策略 `selected=0` 与 K3 真身 8/8 实测一致）。原始逐行输出见 `docs/K2解锁仿真_20260925.md` §10（该文件已合入 main `e8fbccd`）。

复核命令（可直接复跑）：

```
F:/Hermes/hermes-agent/venv/Scripts/python.exe scripts/k2_unlock_sim.py ^
    --db F:/agi/language-genome/data/language_genome.db ^
    --books WK-6e5d2623,WK-dc90993434e9 ^
    --out <临时目录>/k2_unlock_sim_20260925.json

F:/Hermes/hermes-agent/venv/Scripts/python.exe -m pytest tests/test_k2_unlock_sim.py -q
```

> **交付说明（如实）**：本简报制作会话内曾以上述命令尝试现场复跑，Bash 权限门**拒绝**执行 Python（含 pytest 与 sim 脚本，多种调用形式均被拒），故无法附本次会话新时间戳；数值逐条引自同一基线上已合入 main 的那次实跑（同一库、同一脚本、口径未变）。集霸或主控在放行环境复跑上述命令即可逐数对账。

## 1. 8 条策略逐条现状表（真库实跑，2026-09-25T05:35Z）

台账总貌（同一次实跑）：策略 8 条；实例 83 条（全 `status=verified`）；`strategy_conditions` 0 行；`knowledge_packages` 0 行；8 条策略全部 `status=hypothesis | scope=UNCERTAIN | observation_status=observed | effect_status=untested`；基准段实例被 K3 硬剔 82/83 条。

8 条共同真值不再逐列重复（`status=hypothesis`、`scope=UNCERTAIN`、`observation_status=observed`）。S0/S1/S2/S3 verdict 对两本候选世界（`WK-6e5d2623`、`WK-dc90993434e9`）**逐行相同**（证据层不绑被查作品，见 §3①），下表列一次：

| strategy_key | strategy_id | 实例数 | 挂 benchmark 段 | 合格证据<br>`layer2_eligible_instances` | 唯一(根作品,span)<br>`layer2_unique_intervals` | `work_id` 分布 | `role` 分布 | S0 | S1 | S2 | S3 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| legacy:保留原句的递进铺陈 | `ESV2-7cee0b354c34` | 10 | 10 | 0 | 0 | `WK-6c5ea9081547`:6 / `WK-6e5d2623`:3 / `WK-a052258c`:1 | benchmark:10 | `status_not_eligible` | `excluded_no_evidence` | `status_not_eligible` | `excluded_no_evidence` |
| legacy:克制概述vs铺陈渲染 | `ESV2-564ec4504e80` | 11 | 11 | 0 | 0 | `WK-6c5ea9081547`:6 / `WK-6e5d2623`:4 / `WK-a052258c`:1 | benchmark:11 | `status_not_eligible` | `excluded_no_evidence` | `status_not_eligible` | `excluded_no_evidence` |
| legacy:克制留白vs扩写解释 | `ESV2-249d13b06e93` | 10 | 10 | 0 | 0 | `WK-6c5ea9081547`:6 / `WK-6e5d2623`:3 / `WK-a052258c`:1 | benchmark:10 | `status_not_eligible` | `excluded_no_evidence` | `status_not_eligible` | `excluded_no_evidence` |
| legacy:克制直写对话动作 | `ESV2-d014bf02a023` | 9 | 9 | 0 | 0 | `WK-6c5ea9081547`:4 / `WK-6e5d2623`:4 / `WK-a052258c`:1 | benchmark:9 | `status_not_eligible` | `excluded_no_evidence` | `status_not_eligible` | `excluded_no_evidence` |
| legacy:克制直陈vs铺陈注水 | `ESV2-d5beaf06f0bc` | 9 | 9 | 0 | 0 | `WK-6c5ea9081547`:5 / `WK-6e5d2623`:3 / `WK-a052258c`:1 | benchmark:9 | `status_not_eligible` | `excluded_no_evidence` | `status_not_eligible` | `excluded_no_evidence` |
| **legacy:少动作直给疑问** | **`ESV2-0450910b989f`** | **12** | **11** | **1** | **1** | `WK-6c5ea9081547`:7 / `WK-6e5d2623`:4 / `WK-a052258c`:1 | **benchmark:11 / role_null:1** | `status_not_eligible` | `excluded_scope_uncertain` | `status_not_eligible` | **`selected`** |
| legacy:直写本体短推进 | `ESV2-19bfec4ffc65` | 10 | 10 | 0 | 0 | `WK-6c5ea9081547`:5 / `WK-6e5d2623`:4 / `WK-a052258c`:1 | benchmark:10 | `status_not_eligible` | `excluded_no_evidence` | `status_not_eligible` | `excluded_no_evidence` |
| legacy:直抒情绪vs铺陈演绎 | `ESV2-a623fa8ad37b` | 12 | 12 | 0 | 0 | `WK-6c5ea9081547`:6 / `WK-6e5d2623`:5 / `WK-a052258c`:1 | benchmark:12 | `status_not_eligible` | `excluded_no_evidence` | `status_not_eligible` | `excluded_no_evidence` |

唯一合格证据明细（实跑）：实例 `SI-ec30656a1031`，属 `ESV2-0450910b989f`（legacy:少动作直给疑问），根作品＝琼明神女录 `WK-6e5d2623`，挂在 **`role IS NULL` 段**上（既非 `train` 也非 `benchmark`）。覆汉 `WK-dc90993434e9` 在 `strategy_instances` 中 **0 条实例**。

## 2. 三层闸与场景表（固定顺序：层1 status → 层2 证据 → 层3 scope）

1. **层1 status**：`status ∈ eligible_statuses(version)`（当前 = `{verified}`）且 `observation_status ∈ {observed, replicated}`；不满足者**进管道前**被预筛（`status_not_eligible`）。现状 8 条全部死在这一层。
2. **层2 证据**：`_evidence_for` 硬剔 benchmark 源等不合格实例；证据数 = 唯一 (根作品, span) 区间数；0 区间 → `excluded_no_evidence`（**先于层3**）。
3. **层3 scope**：`_scope_matches`；`WORK` 需 `book_id ∈ scope_ids`；`GLOBAL` → `excluded_scope_global_reserved`；其余（含现状 `UNCERTAIN`）→ `excluded_scope_uncertain`。

实跑场景计数（2 本候选世界；「非空包」= `query_knowledge` 返回 matched 且 selected 非空）：

| 场景 | 开哪层 | 非空包 | 逐本 selected | 池化桶（16 策略×本 次） | 哪层在开火 |
|---|---|---|---|---|---|
| S0 | 现状零改动 | 0/2 | 0 / 0 | `status_not_eligible:16` | **层1** |
| S1 | 只升 `status=verified` | 0/2 | 0 / 0 | `excluded_no_evidence:14` + `excluded_scope_uncertain:2` | **层2**（7 条）/ 层3（仅少动作直给疑问 1 条） |
| S2 | 只授 `scope=WORK`+非空依据 | 0/2 | 0 / 0 | `status_not_eligible:16` | **层1** |
| S3 | 层1+层3 同开 | **2/2** | **1 / 1** | `excluded_no_evidence:14` + `selected:2` | 唯一有证据的 1 条出包；7 条仍死层2 |
| S3_no_basis | 同 S3 但依据置空 | 2/2 | 1 / 1 | 同 S3 | **查询层不查依据**：照常出包，只有授予侧形式闸 `grant_scope_valid` 判 False |
| S4_global | 层1+`scope=GLOBAL` | 0/2 | 0 / 0 | `excluded_no_evidence:14` + `excluded_scope_global_reserved:2` | **层3 保留位** |

## 3. 四个语义缺口（逐条写清后果）

① **证据跨作品**：`_evidence_for` 不按 `policy.book_id` 过滤证据。给覆汉 `WK-dc90993434e9` 授 `scope=WORK+scope_ids=[覆汉]` 后，包内容用的是**琼明神女录 `WK-6e5d2623`** 的那条区间。
**后果**：「授予作品 ≠ 证据来源作品」；覆汉的包并非覆汉原生。若不书面裁定接受此语义，任何以「覆汉自己出证据」为前提的验收（K4 试点）都不成立；要同源只能回 K2 补抽。

② **层2 先于层3，单升 `status` 无效**：S1 实跑全 0——7 条卡 `excluded_no_evidence`、1 条卡 `excluded_scope_uncertain`。
**后果**：只裁 `status` 的净收益恒等于 **0 张包**；「先升 status 看看效果」既无收益又制造 `status=verified` 的语义虚标（证据仍全在基准段），污染后续对账口径。

③ **`GLOBAL` 本阶段只预留不授予**：S4_global 层3 对唯一有证据的策略命中 `excluded_scope_global_reserved`。
**后果**：裁 `GLOBAL` = 等价于不动（0 包），且语义上等于跳过逐作品核验；解禁时点与条件须由集霸书面给出，在此之前授予动作不应执行。

④ **S3 的 `1/1` 不是 2 条独立证据**：两本各 1 张包，但引用的是**同一条**唯一 (根作品, span) 区间（根=琼明）。
**后果**：真实证据强度 = **1 条 / 1 区间 / 1 策略（8 条中的 1/8）**，不是「2 张包 = 解锁 2 项」。宣传口径与验收口径都必须按 1 条计。

## 4. 候选裁定 A / B / C（结论留空，勾选区在文末）

| 选项 | 内容 | 收益（实跑口径） | 代价 | 前置条件 | 可逆性 |
|---|---|---|---|---|---|
| **A 不动** | 保持 `hypothesis/UNCERTAIN` 冻结 | 0 包（S0）；K3 真身 8/8 空返回，`knowledge_packages` 恒 0 行——最小闭环继续死在层1 | 无解锁、无试点数据；审计 P0-1/P0-2 维持未决 | 无 | 完全可逆（零改动） |
| **B 只升层1** | 8 条 `status→verified`，scope 不动 | **0 张包（实跑铁证：S1 全 0）** | 纯语义虚标：无证据支撑的 `verified` 标记扩散到 7 条无合格证据的策略；后续任何「已 verified」口径统计失真 | 无（但不满足层2/层3） | 单字段可回退；但期间下游若已按 `verified` 消费需逐笔作废 |
| **C 层1+层3 同开** | 对**逐条点名**的策略升 `status=verified` **且**授 `scope=WORK + scope_ids + 非空 scope_basis` | 上限 **selected=1/本**（唯一受益者＝legacy:少动作直给疑问；证据 1 条、根作品=琼明）；7 条仍 `excluded_no_evidence`，不随拍板回流 | §3① 跨作品语义、§3④ 单证据强度必须如实写进依据；覆汉 `allowed_purposes=[]` 若上游收紧则该 2 张包归零 | ①§6 未决问题 Q1/Q2/Q5 先有书面答复；②授 `WORK` 不授 `GLOBAL`（Q3）；③出包落库另走 K3-B `freeze_package`（本仿真零写库）；④K4 只读预检复跑通过才谈真跑 | 字段本身可回退；但包一经冻结并被 K4/写作侧消费，只能事后作废、不能追溯抵消影响 |

**红线（三个选项共用）**：**不得批量改 `status` 冒充验收。** 8 条一起刷 `verified` 不产生任何一张包（S1=0），却把「未验证」伪装成「已验证」——这是验收造假，不是解锁。任何改动必须逐策略点名、附 `scope_basis` 书面依据、与 §6 裁定记录一一对应。

## 5. 建议的最小闭环顺序（每步的验收证据）

1. **拍板**（集霸，文末勾选区）→ 证据：勾选区签字/书面记录 + 逐策略裁定清单（授谁、何 scope、依据文本）。
2. **非 benchmark 试点扩量**（K2 侧写操作：补抽/段落 role 标全，使目标策略出现 `train` 段合格实例）→ 证据：只读复跑 `k2_unlock_sim.py` 台账中目标策略 `layer2_eligible_instances > 1` 且 `layer2_root_works` 含试点本；`role_null` 证据归零或获 Q5 追认。
3. **K3 非空包**（按冻结流程 `freeze_package` 落库）→ 证据：`knowledge_packages` 行数 > 0，`package_sha256`/`snapshot_fingerprint` 与仿真预测对账一致。
4. **K4 只读预检**（不花模型额度）→ 证据：预检报告通过；额度/预算上限按 Q4 批复值设闸；覆汉 `allowed_purposes` 检查有明确结论。
5. **才谈 K4 真跑** → 证据：在批复额度内的跑单 + 输出可溯源到已冻结包。

顺序不可倒置：跳过 2 直接做 3，得到的只是 §3④ 那条单证据包；跳过 3 直接真跑，无冻结包可溯源。

## 6. 未决问题清单（需集霸书面答复）

| # | 问题 | 背景锚点 |
|---|---|---|
| Q1 | **证据跨作品是否接受**：向覆汉授 WORK 时，用琼明的区间出包算不算有效试点？（若否 → 先补抽，见 §5 步 2） | §3① |
| Q2 | **`scope_basis` 是否必须非空才可授予**：现状查询层不查依据（S3_no_basis 照常出包），是否要求运行时也拒空依据，还是仅约束授予侧形式闸？ | §2 S3_no_basis |
| Q3 | **`GLOBAL` 何时解禁**：解禁条件（策略数/证据数门槛）与责任人口径 | §3③ |
| Q4 | **K4 真跑的额度上限**：单本预算、并发闸、超限时熔断责任 | §5 步 4 |
| Q5 | **`role IS NULL` 段算不算生产证据**：唯一合格区间正挂在这种段上；裁「不算」则 S3 收益归零（0 张包），唯一出路是先补抽/标全 role | §1 明细 |
| Q6 | **`effect_status=untested` 是否阻断出包**：包内只有「观察过」没有「效果被证实」，写作侧消费是否需附此标注 | §1 台账 |
| Q7 | **覆汉 `allowed_purposes=[]`**：零用途授权下是否允许其被授 WORK / 进 K4 真跑 | §4 C 代价 |
| Q8 | **7 条无合格证据策略的处置**：维持冻结，还是限期补抽考核表？ | §2 S3 桶 |

## 7. 集霸勾选区（结论由此处产生，本简报不代裁）

裁定人（朱十一）：＿＿＿＿＿＿＿＿ 日期：＿＿＿＿＿＿ 签字：＿＿＿＿＿＿

- [ ] **选 A**：8 条维持冻结（知悉：闭环继续停在层1）
- [ ] **选 B**：只升 status（知悉：实跑收益 = 0 包；勾选即须附「不构成解锁」标注）
- [ ] **选 C**：层1+层3 同开，逐条点名：`ESV2-＿＿＿＿＿＿＿＿＿＿`（可多条，但每条须单写依据）
- Q1 跨作品证据：□接受 □不接受 □仅限＿＿＿
- Q2 `scope_basis`：□必须非空 □仅授予侧约束
- Q3 `GLOBAL` 解禁条件/时点：＿＿＿＿＿＿＿＿＿＿
- Q4 K4 真跑额度上限：＿＿＿＿（token/日）；熔断责任人：＿＿＿
- Q5 `role_null` 段：□算生产证据 □不算（则 C 收益归零）
- Q6 `untested` 标注：□必须随包透传 □不强制
- Q7 覆汉零授权：□先补登记用途再跑 □豁免 □不触碰
- Q8 其余 7 条：□维持冻结 □限期补抽（期限：＿＿＿）
- 依据文本（若选 C，逐条抄录进 `scope_basis` 的内容）：＿＿＿＿＿＿＿＿＿＿＿＿

> 收尾口径：无论勾选哪项，改真值由集霸授权的写路径执行并留痕；本简报与所引仿真全程 `mode=ro`、零写库、零模型调用。
