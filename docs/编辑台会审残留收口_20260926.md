# 编辑台会审残留收口（gui-editorial-residual）交付文档

- 日期：2026-09-26
- 会审：`F:/Hermes/team/reviews/language-genome-87bd95235d.md` 一般级 4 条（经主控复核成立、均非阻塞）
- 工作树：`F:/agi/_scratch/worktrees/editorial-residual`（分支 `fix/editorial-residual`，基线 bd256c1）
- 执行者：ZCode（集霸指派，接手 Codex 故障期间的该任务单）

---

## 一、现状核对结论（先核对、后动手）

| 会审条目 | 核对结果 | 本次动作 |
|---|---|---|
| #1 enhanceNav 硬编码 3 组导航，`[i]` undefined 抛 TypeError → 整页初始化中断 | **bc23a8f 已修**：`navLists[i]` 处已有 `if (!target) return;` 存在性守卫 | 代码零改动；补两端边界回归（lists=0 / lists=5）钉住现状 |
| #2 refresh 失败分支向 `#banner` 重复追加「重新读取」按钮 | **bc23a8f 已修**：`!banner.querySelector('.lg-banner-retry')` 去重守卫在位 | 代码零改动；补「初载+2 次点击重试共 3 连败恒 1 按钮、fetch 恰 3 次」回归 |
| #3 `openDoneRecords`/`toggleQueue` 用 `$('#rv-q-wrap').style.display === 'none'` 判可见性，class/CSS 隐藏时恒 false | **仍在**（index.html 865/1314 两处） | **本次修复**：改 `getComputedStyle(...).display` 判定 |
| #4 两份 editorial.css 同名不同口径（120 行 vs 78 行，`:root` 变量命名/值不一致），无同步校验 | **仍在**，且实锤漂移现行（见三） | **本次修复**：语义层结构化为「刻度+别名」+ 机械对账回归 |

「若两者本就是源→产物关系而非副本」的如实认定：**两份文件既不是副本、也不是生成关系**，而是
同一调色板、服务两个页面族（websrc 十页编辑档案室走 `/lab/_shared/editorial.css`；
app 盲评台走 `/static/editorial.css`）的**兄弟皮肤**。tokens.css 已声明「原语层（--n-*/--acc-*）
→ 语义层（--bg/--surface/…）」的两层令牌架构，app 版此前用裸值绕开了该架构。本次修复即
把两份 editorial.css 的语义层全部结构化为刻度别名。**若未来统一为单源/生成关系，本条回归
应改为「生成脚本 + 校验」**（按任务单要求如实写明）。

## 二、修复明细

### 2.1 index.html（#3，2 处）

- `openDoneRecords()`：`$('#rv-q-wrap').style.display === 'none'` → `getComputedStyle($('#rv-q-wrap')).display === 'none'`
- `toggleQueue()`：`w.style.display === 'none'` → `getComputedStyle(w).display === 'none'`
- C3：未引入任何新 `innerHTML`（两处均为判定表达式替换）。

### 2.2 app/static/editorial.css（#4，语义层结构化）

- `:root`（light）新增完整刻度 `--n-0…--n-950` + `--acc-100…--acc-700`（与 websrc 版逐值一致）；
  13 个语义变量全部改为 `var()` 别名：`--bg:var(--n-100)`、`--surface:var(--n-0)`、
  `--surface-2:var(--n-50)`、`--line:var(--n-300)`、`--line-2:var(--n-400)`、`--fg:var(--n-900)`、
  `--fg-2:var(--n-700)`、`--dim:var(--n-600)`、`--accent:var(--acc-500)`、`--accent-soft:var(--acc-100)`、
  `--accent-strong:var(--acc-600)`、`--paper-bg:var(--n-0)`、`--ctx-fg:var(--n-600)`。
- `html[data-theme="dark"]` 只覆写刻度与阴影，语义别名随主题自动解析（CSS 自定义属性求值时机）。
- **对齐带来的渲染值变化（改前 → 改后，值域本次实跑核对、像素渲染未自跑）**：
  - light：仅 `--ctx-fg` `#666b61 → #6b6c63`（≈1/255 级差），其余 12 项值不变；
  - dark：`--bg` `#111712 → #141a15`、`--surface` `#1c231d → #202620`、`--surface-2` `#202920 → #1a211b`
    （明暗层级与 light 侧同向，此前 app 暗色 surface-2 反向偏亮）、`--line` `#354036 → #354038`、
    `--line-2` `#465348 → #4a544a`、`--ctx-fg` `#b8bcb1 → #a0a99e`；
  - `--radius`/`--shadow-*` 非 palette 量保持原值。
- 保留裸值的非刻度量（有意）：`--shadow-sm/--shadow-card/--shadow-dock`（app 专属投影）、
  `--radius`；websrc 侧 `--row-hover`、`--nav-*`（导航专属，不入调色板）。

### 2.3 websrc/_shared/editorial.css（#4，暗色块口径对齐）

- 暗色块语义覆盖 `--bg/--surface/--surface-2/--line/--line-2/--fg/--fg-2/--dim` 由裸值改 `var()` 别名，
  映射与 app 侧同表。改前裸值与自身刻度的漂移实锤：`--line:#354036` ≠ `--n-300:#354038`（末位差）、
  `--bg:#111712`/`--surface:#1c231d`/`--surface-2:#202920`/`--line-2:#465348` 均不在刻度内。
- 对齐后 websrc 暗色渲染值变化：与上表 dark 6 项相同（值域本次实跑核对、像素渲染未自跑）。

### 2.4 websrc/_shared/editorial.js（#1/#2）

零改动（bc23a8f 已修，核对结论见一；反向验证 M3 曾临时改写、已精确恢复，最终 md5 与基线一致）。

## 三、交付物

1. `tests/test_editorial_residual.py` —— **6 个测试函数**（本次实跑全绿）：
   1. `test_enhance_nav_survives_zero_and_extra_nav_lists`（钉 #1：lists=0/5 不炸、rail 构建、分组标题恰 3）
   2. `test_banner_retry_button_dedup_across_repeated_failures`（钉 #2：**去重**——3 连败按钮恒 1、fetch 恰 3 次）
   3. `test_toggle_queue_both_directions_use_computed_style`（钉 #3：双向开合 + 静态钉 `getComputedStyle`）
   4. `test_open_done_records_expands_hidden_queue_and_never_collapses`（钉 #3：内联隐藏展开 / class 隐藏展开 / 可见不收起）
   5. `test_editorial_css_palette_scale_identical_across_two_files`（钉 #4：两份 CSS 刻度逐值一致，light+dark）
   6. `test_app_css_semantic_vars_are_scale_aliases`（钉 #4：13 语义变量必为 var() 别名、映射同权威表、websrc 暗色同表）
2. `docs/编辑台会审残留收口_20260926.md`（本文档）。
3. 代码变更：`app/static/index.html`（+5/-2）、`app/static/editorial.css`（+19/-7）、
   `websrc/_shared/editorial.css`（+5/-2）、`websrc/_shared/editorial.js` 零改动。

## 四、反向验证（承重变异，3 条，本次全部实跑）

方法：改被测代码 → 指定用例转红（rc=1）→ 精确恢复 → md5 对账一致 → 复绿（rc=0）。
执行器 `F:/agi/_mut_check.py`（一次性脚本，跑批时 LG_LOCK_DIR 指向临时目录）。

| # | 变异（被测代码） | 转红用例 | 变异 rc | 恢复后 md5（前=后） | 复绿 rc |
|---|---|---|---|---|---|
| M1 | `websrc/_shared/editorial.css` light `--n-100:#f2eee5 → #f2eee6`（刻度漂移） | test_editorial_css_palette_scale_identical_across_two_files | 1 | `b4f19007a16d` | 0 |
| M2 | `app/static/index.html` `getComputedStyle($('#rv-q-wrap')).display` → `$('#rv-q-wrap').style.display`（判定回退） | test_open_done_records_expands_hidden_queue_and_never_collapses | 1 | `827d527fd012` | 0 |
| M3 | `websrc/_shared/editorial.js` 去重守卫 `if (banner && !banner.querySelector('.lg-banner-retry'))` → `if (banner)`（**去重**失效） | test_banner_retry_button_dedup_across_repeated_failures | 1 | `0c7e31a5d2f2` | 0 |

三条全部「红 → md5 一致 → 绿」，`REVERSE_VERIFY: ALL_PASS`（本次实跑）。

## 五、门命令结果（本次实跑）

```
F:/Hermes/hermes-agent/venv/Scripts/python.exe -m pytest \
  tests/test_editorial_residual.py tests/test_editorial_js.py \
  tests/test_websrc_contract.py tests/test_frontend_escape.py -q
```

- rc=**0**，**38 passed**（4+4+18+10 席位：residual 6 + editorial_js 4 + websrc_contract 18 + frontend_escape 10）。
- C1 核对：三个既有门文件**只增不减**（本次未删除/未改写任何既有用例，编辑台相关既有 32 条全部仍绿）。

## 六、C1–C4 契约自检

| 契约 | 自检 |
|---|---|
| C1 既有门不放松 | rc=0；residual 之外 32 条既有用例全绿，未改任何既有测试（本次实跑） |
| C2 机械校验真能抓漂移 | M1（刻度 hex 漂移）实跑转红；语义映射漂移/裸色值回归分别由 test 6 的映射断言与裸值断言钉住（映射/裸值两条变异未单独实跑，逻辑上由同一断言路径覆盖——如实标注） |
| C3 动态文本 textContent/属性 API | 本次零新增 innerHTML；测试桩断言渲染走 textContent |
| C4 单文件面 | 仅触碰任务单授权的 4 文件 + 新增 1 测试文件 + 本文档；`app/api.py`、`scripts/k5_rereview_driver.py` 零触碰（git status 佐证，本次实跑） |

## 六·补 会审 e029d88 BLOCK 两条严重的整改（主控亲修，2026-09-26 21:2x）

会审双席对**合入前**的 8361456 判 BLOCK，两条严重成立（我逐条回源码复核后确认）：

1. **严重①** `toggleQueue` 展开用 `w.style.display = ''`：清空内联样式**压不过** class/样式表的
   `display:none` ⇒ 文案已翻「收起」而元素实际仍隐藏，正是会审 #3 要修的症状在真实 DOM 里依旧存在。
   **已修**：改为 `w.style.display = open ? 'block' : 'none'`（显式 block 才能真展开）。
2. **严重②** 场景 B 用例的 `getComputedStyle` 桩把 computed 固定成 `_computed`，不随内联样式重算，
   且断言只看 label/tabs、不看最终可见性 ⇒ **回归恒绿但钉不住修复点**。
   **已修**：桩改为真实语义 `el.style.display || el._sheetDisplay || 'block'`（内联非空则胜出，
   否则回落样式表规则），并在 A/B/E 三处补 `computed != 'none'` 与 `display == 'block'` 的行为断言。

**反向验证（本次实跑，主控亲做）**：

| # | 变异 | 转红用例 | 变异 rc | 恢复 rc | 恢复后 md5 |
|---|---|---|---|---|---|
| MUT1 | `display = open ? 'block' : 'none'` → `'' : 'none'`（严重①原缺陷） | test_open_done_records_expands_hidden_queue_and_never_collapses | 1 | 0 | `3fea940fa298`（cmp 逐字节一致） |
| MUT2 | `getComputedStyle(w).display` → `w.style.display` | test_editorial_residual 两条可见性用例 | 1 | 0 | `3fea940fa298`（cmp 逐字节一致） |

门复跑：`rc=0，38 passed`（同一条门命令）。**结论：两条严重均已真修且由行为断言钉住。**

## 七、残留（如实）

1. **像素级渲染未自跑**：CSS 对齐的所有渲染值变化仅做了值域核对（测试断言），未起浏览器截图比对；
   dark 侧 `--surface-2` 层级方向变化（与 light 同向）建议下次开盲评台时目验一次。
2. `index.html` 中 `#tab-review` 的 `style.display === 'none'`（1237/1256/1267 三处，本次实跑 grep 计数）
   属同类潜在问题，但该元素始终由内联样式切换、现状无触发路径，且不在会审条目内——按 C4 最小面**未动**。
3. `tokens.css` 暗色块语义层仍是裸值（#0c0c0e 等），与 editorial 刻度是另一套暗色系——tokens.css
   不在本任务单 4 文件之内，未动；两页面族 light 背景 `--bg` 实际来源不同（tokens vs editorial 覆盖）
   的跨文件语义一致性问题同样留待 tokens 收口时处理。
4. M2/M3 之外的变异组合（如同时漂移多值、映射互换）未逐一实跑，由同一断言路径覆盖（本次未自跑）。
5. `--serif/--sans/--mono` 等排版令牌不在本条 scope，未核对两页面族一致性（未自跑）。

## 八、纪律声明

未 commit / 未 merge / 未 push（工作树留待主控验收）；未改真库；未动网络（node/pytest 全本地）；
做完即停。
