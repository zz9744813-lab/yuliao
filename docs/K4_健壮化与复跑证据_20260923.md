# K4 健壮化与复跑证据（2026-09-23，正式任务件）

- 复跑命令（原样可重跑，密钥经 env 注入不入命令行）：
  `K4_ALLOW_LIVE=1 LG_LLM_MODE=real LG_GATEWAY_BASE_URL=http://127.0.0.1:4000/v1
  LG_GATEWAY_API_KEY=<F:/Hermes/secrets/litellm_master_key.txt>
  python scripts/k4_paired_scenes.py --live --writer-model deepseek-v4.1-flash
  --verifier-model deepseek-v4.1-flash --out out_k4_3_mc22_v2 --channel-changed`
- 模型名**无 vendor 前缀**（`deepseek-v4.1-flash`）：带前缀的
  `deepseek/deepseek-v4.1-flash` 实测 HTTP 200 但 content 空串（静默空输出），
  不得使用（任务书 3）。真跑前 pool_probe 探活：
  deepseek-v4.1-flash@LiteLLM = **ok**（length_capped——探针自设 1-token
  上限触发 A06 完成门，生成本身已发生=通道活；R8 闸过）。
- 产物：`out_k4_3_mc22_v2/k4_paired.json`（新目录，未覆盖 out_k4_3 /
  out_k4_3_mc22）；`worlds_dir=C:\Users\6\AppData\Local\Temp\k4_worlds_0taf6d2a`
  （live 留库作收据——tokens 对账数据源，已记入产物）。

## 1. 三场逐臂结果

| 场 | A 臂（v2 空包*） | B 臂（空包对照） |
|---|---|---|
| s1 | **committed**（88 字正文，2 调用 5.5s，tokens 1063） | **committed**（86 字，2 调用 6.0s，tokens 1104） |
| s2 | failed：**call_budget_exhausted**（6 次成功调用后第 7 次被预算闸拒） | failed：**rewrite_budget_exhausted: missing_or_unplanned_event, state_patch…**（6 次调用全成功，改稿轮 2 用尽——verifier 连续判缺事件/未授权状态改写，闸守住了） |
| s3 | skipped_after_failure（断臂即停，不烧调用） | skipped_after_failure（同左） |

\* A 臂知识包 n_techniques=0 为**预期**：8 张策略卡经 3 席×8=24 次独立判定
全部「证据不支持+互相重复」（docs/策略语义审查_判定模型版_20260923.md，0 条
升格），K3 只服务 verified+observed——本轮不放宽门禁、不改 status（任务书纪律）。
A/B 两臂在 s1 的对照即「空包 vs 空包」结构验证：机制先于知识打通。

## 2. 失败链（本次实测）

- **s2 A 臂**（6162 tokens 后被预算闸拒）：writer.0 ✅ → verifier.0 ✅ →
  writer.1 ✅ → verifier.1 ✅（判 hard issue）→ **verifier.1.contract1 ✅**
  （核验工件返修：评审引用合同缺陷，正文不动——A10 口径）→ writer.2 ✅ →
  第 7 次（verifier.2）**call_budget_exhausted**（Budget.max_calls=6，闸在
  插入前拒——被拒尝试不留行）。
- **s2 B 臂**（4481 tokens，6 调用全成功）：三轮 writer/verifier 后
  **rewrite_budget_exhausted**——verifier 连续判 missing_or_unplanned_event +
  state_patch_not_authorized_by_plan（写作模型三稿都没满足计划的事件/状态
  契约，核验器不放行）。这是**质量信号**（deepseek-v4.1-flash 作 writer 在
  s2 场景三稿不达标），不是机制故障；预算闸/改稿闸/断臂全部按设计工作。
- **verifier 空响应重试（健壮化 1）本轮未触发**：全程 0 次
  gateway_invalid_or_partial_result（retried=False × 全部收据；LiteLLM/
  deepseek 通道本轮无空响应）。机制侧有单元回归钉死：重试救轮+留痕
  （verifier.0.failed + verifier.0.retry，收据 verifier_invalid_retries=1）/
  双无效 fail-closed / 预算闸不豁免（重试那次同样过 call 预算）
  （tests/test_scene_runtime.py 四例，commit 6d35bf8）。

## 3. tokens 记账核对（健壮化 2）

修复：store.usage 聚合**成功调用**的网关实账 tokens（此前恒 0——失败调用的
上游计费本侧不可见，网关账为交叉核对侧）。

| 对账项 | 收据 | calls 表（worlds_dir/arm*/k4.sqlite） | 判定 |
|---|---|---|---|
| s1 A（job scene-9d834…） | 1063 | 1063 | **MATCH** |
| s1 B（job scene-db5ac…） | 1104 | 1104 | **MATCH** |
| 失败臂 tokens（无收据，按设计只入 failures+calls 表） | — | A s2 6162 + B s2 4481 = 10,643 | 可查（worlds_dir 留库） |
| **本次真跑总实耗** | 收据合计 2,167 | **12,810** | 全量可核 |

## 4. 与上一轮 out_k4_3_mc22 的差异对照

| 维度 | 上一轮（out_k4_3_mc22，主控取证） | 本次（out_k4_3_mc22_v2） |
|---|---|---|
| 通道 | mc22（http，无 TLS） | LiteLLM 本地代理 127.0.0.1:4000，双角色 deepseek-v4.1-flash（无前缀名） |
| s1 | B committed；**A 失败：verifier.2 空响应（gateway_invalid_or_partial_result, 16.8s）烧掉改写链** | **A/B 双 committed**（空响应重试机制在位；本轮通道无空响应，未触发） |
| s2/s3 | B committed（B 三场全过）；A 断臂后 skipped | 双臂诚实失败（预算闸/改稿闸各一），s3 双臂 skipped——**无幻影**（skipped 单列） |
| 收据 tokens | **恒 0**（记账 bug） | 真实数字，逐 job 与 calls 表 MATCH（§3） |
| 收据字段 | 无 channel_changed/models/retried/worlds_dir | 全带：channel_changed=true（C5）、models={writer,verifier}、gateway_host=127.0.0.1:4000、retried、verifier_attempts 逐次、worlds_dir |
| 改写链保护 | verifier 无效直接烧配额 | 无效不烧轮、同角色重试 1 次（stage+'.retry' 留痕）+fail-closed |

## 5. 结论

K4 机制本身（预算闸、改稿闸、核验工件返修、断臂即停、空响应重试、tokens
记账、通道/模型留痕、C5 标记）**健壮、可复跑、账目可核**；A 臂产出质量依赖
策略卡修好后的非空知识包（K2-CARDS-MERGE 线：合并 S1/S2 + 非 benchmark 成对
对照证据 + 两席判定），本轮未动任何门禁与 status。
