# K4 验收定性（2026-09-25，lg-k4-accept-harness）

- 工具：`scripts/k4_accept_report.py`（只读收据/世界库，mode=ro；零模型
  调用；机械判据不靠人读日志）+ `tests/test_k4_accept_report.py`
  （7 例全离线：预算闸拒⇒诚实失败 / 缺收据⇒证据不足两红线，另钉自洽破
  ⇒缺陷、rollback 炸⇒缺陷、错误清单空⇒缺陷、未知码⇒缺陷、世界库不可读
  ⇒证据不足）。
- 收据源（自动取最新）：`out_k4_3_mc22_v2/k4_paired.json`；
  世界库：`C:\Users\6\AppData\Local\Temp\k4_worlds_0taf6d2a\arm{1,2}\k4.sqlite`
  （live 留库收据，只读打开）。

## 0. 实跑命令与退出码

```
F:/Hermes/hermes-agent/venv/Scripts/python.exe scripts/k4_accept_report.py \
  --repo-root F:/agi/language-genome --out k4_accept_report_20260925.json
→ REPORT_EXIT=0
```

## 1. 三场逐场验收表（真跑输出）

| 场景 | 臂 | 结果 | 判定 | 依据（机械，逐行） |
|---|---|---|---|---|
| s1 | A | committed | **通过** | prose.status=committed；job=scene-9d834…；usage calls=2/tokens=1063；live=True，channel_changed=True |
| s1 | B | committed | **通过** | 同形；job=scene-db5ac…；usage calls=2/tokens=1104 |
| s2 | A | failed | **诚实失败** | error=call_budget_exhausted；rollback_failed=False；世界库 job=scene-02a05… status=running，**calls=6（max_calls=6）全 succeeded**——预算闸在第 7 次调用前拒，自洽 |
| s2 | B | failed | **诚实失败** | error=rewrite_budget_exhausted:**missing_or_unplanned_event,state_patch_not_authorized_by_plan**；rollback_failed=False；世界库 job=scene-4aef… calls=6/6 全 succeeded，核验错误清单非空（verifier 三轮不放行），自洽 |
| s3 | A | skipped | 连锁跳过 | skipped_after=s2（断臂即停，不烧调用；随 s2-A 定性） |
| s3 | B | skipped | 连锁跳过 | skipped_after=s2（随 s2-B 定性） |

汇总：pass=2，**honest_failure=2**，defect_undetermined=0，
insufficient_evidence=0，cascade_skip=2（total 6）。

## 2. s2 定性结论

**s2 双臂均为「诚实失败」——不是口径缺陷**（机械判据三连逐条成立）：
闸拒绝码 ∈ {call_budget_exhausted, rewrite_budget_exhausted}；
无产出（无 committed 正文）；收据/世界库自洽（calls==max_calls=6 且
全部 succeeded、job 非 committed、回滚干净、B 臂失败串带非空核验错误
清单）。预算闸/改稿闸/断臂机制全部按设计工作——**验收口径与闸门本身
没有缺陷**。

但要分清：**诚实失败 ≠ 验收通过**。K4 三场验收判据（K5-A P1：
6/6 committed，failures=0）仍未满足——s2 A 臂改稿链 6 次调用（含 1 次
核验工件返修 verifier.1.contract1）后预算耗尽未收敛；s2 B 臂 writer
（deepseek-v4.1-flash）三稿连续未满足计划事件/状态契约被 verifier
诚实拦下。**根因归写作侧能力/预算配比，非闸门口径**：
- A 臂：Budget.max_calls=6 下，含 contract 返修的改稿链用满 6 调用仍
  未过——预算对 s2 场景复杂度偏紧；
- B 臂：writer 三稿 missing_or_unplanned_event +
  state_patch_not_authorized_by_plan——模型未按计划事件写作（场景
  契约遵从不足）。

## 3. 需主控拍板的点

1. **预算配比**：s2-A 显示 6 调用/臂对含返修的改稿链偏紧。是否上调
   K4 live 的 Budget.max_calls（如 8）属判据/成本权衡——须主控拍板
   （本任务不擅改；K5-A §4 的自洽推导按 6/臂给出，上调需同步复核
   成本模型与止损线）。
2. **writer 场景契约遵从**：s2-B 三稿被拦提示 writer 提示词或模型
   选择需校准（是否换 writer 模型/强化计划事件指令）——动到模型基线
   须按 C5 通道纪律标记并复核，属主控决策。
3. **上游仍是第一卡点**：A 臂知识包全空（策略卡晋升链未走完）不因
   本定性改变；K4 机制验收可判「闸门诚实」，K5 扩场判据仍被 P2/C3
   拦住（见 docs/K5判据核验_20260925.md）。

## 4. 边界声明

- 本报告只读（收据 JSON + 世界库 mode=ro），无写操作、无 --live、
  未清语料、未合并未推送；
- 判定为机械判据输出，不覆盖「预期产出质量」判断——质量结论属
  人工/主控侧。
