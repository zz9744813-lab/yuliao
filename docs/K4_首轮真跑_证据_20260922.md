# K4 三场配对 A/B · 首轮真跑证据（2026-09-22 23:58，主控实跑）

> 结论一句话：**真跑通道打通了，但实验数据不可用**——A 臂知识包全空（n_techniques=0），
> 且 6 条失败里 4 条是前场连锁幻影。真缺陷不在钱，在**通道健康**与**缺失的 K2-A 放量驱动**。

## 1. 通道实测（同口径 = max_tokens 3000 + 长上下文 + temperature 0.65）

| 通道 | 结果 | 备注 |
|---|---|---|
| 中转 `107.172.138.14:3000`（.env 现值） | ❌ 120s 无响应（超时） | 本轮 `gateway_http_500` 的直接来源 |
| `mc22.top:8899`（清洗高速通道） | ✅ HTTP 200，5.9s，540 tokens，finish=stop | 真跑路由改用此通道 |
| 中转 verifier `z-ai/glm-5.3` | ✅ 3.7s（短请求） | cross-family 验证器可用 |
| `minimaxai/minimax-m3` | ❌ HTTP 410 已下架（2026-09-09 EOL） | 池内残留死名 |
| `moonshotai/kimi-k3` | ⚠️ 98.4s（可用但慢） | 不作真跑首选 |

**旧记录已失效**：`docs/K5-A_离线前置评估.md` §2 的 P0「deepseek 402 资金墙」——
2026-09-23 00:0x 单发探针实测 `deepseek-v4.1-flash` / `deepseek-flash` 均 **HTTP 200**，
资金墙不成立；真正挡住真跑的是**中转对长文生成超时**。P0 应改判为「通道健康」而非「资金」。

## 2. 首轮产物（`out_k4_3/k4_paired.json`，live=true）

- 三场 A 臂 `n_techniques` **均为 0** ⇒ 判据 C3（A 臂非空包率 ≥2/3）**必然不过**；
- failures=6：`s1` 双臂 `gateway_http_500`（真因：通道超时）；`s2/s3` 双臂 `world_revision_conflict`
  ⇒ **连锁幻影**：`SCENES = [(s1,rev0),(s2,rev1),(s3,rev2)]` 是硬编码版本阶梯，
  s1 失败后世界停在 rev0，s2/s3 的 `expected_revision` 永远对不上。**真实独立失败只有 1 类**，
  但台账按 6 条记 ⇒ C2「零失败臂」判据会被幻影污染。

## 3. 根因与修补（已派 GUI 线，任务 id：K2-A-LIVE-放量驱动 / K2-A-单段live探针）

1. **关键路径缺口**：`app/knowledge_extract.py`（含三道门）**全仓无调用方**——
   K2-A「实例抽取放量」驱动脚本尚未存在 ⇒ 知识库 v2 实例恒为 0 ⇒ 包恒空 ⇒ C1 证据无从产生。
   ⇒ 新写 `scripts/k2_extract_backfill.py`（幂等 / 预算闸 / --dry-run 零库写 / --limit）。
2. **口径缺陷**：`scripts/k4_paired_scenes.py` 须把「前场失败导致的后续冲突」标
   `skipped_after_failure`，使台账可区分真失败与连锁。
3. **通道**：真跑走 mc22（env 覆盖 `LG_GATEWAY_BASE_URL`，config 用 setdefault，不改 .env），
   收据/产物标 `channel_changed=true`（C5 口径）；writer=`deepseek-v4.1-flash`，verifier=`z-ai/glm-5.3`。

## 4. 复现命令（原样可跑）

```
# 通道探针
python - <<'PY'
import json,urllib.request,time
key=open(r"F:\Hermes\secrets\mc22_key.txt",encoding="utf-8").read().strip()
body=json.dumps({"model":"deepseek-v4.1-flash","messages":[{"role":"user","content":"场景卡"*200}],
                 "max_tokens":3000,"temperature":0.65}).encode()
r=urllib.request.Request("http://mc22.top:8899/v1/chat/completions",data=body,
    headers={"Content-Type":"application/json","Authorization":"Bearer "+key})
print(urllib.request.urlopen(r,timeout=120).status)
PY
# 三场真跑（换通道，须新目录，不许覆盖）
cd F:/agi/language-genome
K4_ALLOW_LIVE=1 LLM_MODE=real \
LG_GATEWAY_BASE_URL=http://mc22.top:8899/v1 LG_GATEWAY_API_KEY=<mc22_key> \
python scripts/k4_paired_scenes.py --live --writer-model deepseek-v4.1-flash \
       --verifier-model z-ai/glm-5.3 --out out_k4_3_mc22
```

*记录人：Hermes（主控）· 证据来源：本轮实跑 stdout、网关探针、git 9350f99 工作区*
