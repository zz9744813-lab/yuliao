# K5 晋升接线探针（lg-promotion-wiring-probe，2026-09-25）

对应派工：复核报告 `k5_freeze_adversarial_20260925.md` §3.2 A4。
探针脚本：`scripts/k5_promotion_wire_probe.py`；回归：
`tests/test_k5_promotion_wire_probe.py`（13 例，全离线）。

**本文档的证据分级（先看这个再看结论）**：
- 【静态·可复算】= 由本 worker 在本工作树当场 grep 得到，任何人可复算，
  行号见文中标注；
- 【主仓记录·转述】= 转述 `docs/K5晋升链接线_20260925.md`（主控已实测
  落档的真库读数），本 worker **未复核**其取数过程；
- 【未自跑】= 本 worker 会话中因权限拒绝无法执行、如实标注（见 §6）。

---

## 1. 探针要回答的四件事与口径

| 节 | 回答 | 口径 |
|---|---|---|
| ① `c3_p2_inputs` | 核验器 C3/P2 的输入（A 臂包）到底从哪来 | 静态（消费字段→产出方脚本:行号）+ 动态（真跑 `k5_criteria_check.build_report` 一次，C3/P2 条目原样落盘） |
| ② `writers` | `knowledge_packages` / `strategy_conditions` 的每处写入口，及「无人工步骤可达性」 | 全仓 grep 命中点逐个分类（test_scaffold / runtime_contract / gated_writer / library_writer / candidate_writer） |
| ③ `chain` | 门到门五段链断在哪一段 | 真库 `mode=ro` 逐段非空读数；**撞到第一条不满足的段就停下**，不跳段猜 |
| ④ `a4` | 35/48 过门对是否已在库 | pairs 文件行数 + 旁路账本 `gates_ok`/`persist_outcome` + `strategy_instances(extractor_model='paired_contrast_v2')` SQL 行数，三路并查 |

五段链（③）：`expression_strategies_v2 卡行 → K3 可服务卡
（status∈ELIGIBLE_STATUS 且 observation∈ELIGIBLE_OBSERVATION，单源
import）→ strategy_conditions → knowledge_packages → 过门对在库
（≥35 行）`。断点判定词表只有四个：
`卡未晋升 / K3 过滤 / 写入路径缺失 / 证据不足`；任何读数取不到一律
「证据不足」，**不许**折叠成前三者之一。

## 2. 结论①：C3/P2 消费的是 K4 收据 JSON，不是 knowledge_packages 表

【静态·可复算】消费侧（`scripts/k5_criteria_check.py`，本工作树行号）：

| 字段 | 消费点 |
|---|---|
| `packages = a.get("packages")`（收据 JSON 的包列表本体） | `k5_criteria_check.py:197` |
| `p.get("n_techniques", 0) >= 1`（非空判定） | `:207`（P2 主体 `:263-265`，C3 复用同口径 `:306-309`，两条判据文字均在 `:180/:185` 声明） |
| `pkg_scenes = len(packages)`（场数口径） | `:208` |
| `p.get("arm") == "A"`（promotion_gap_preflight 的 A 臂过滤） | `:519-520`（函数定义 `:471`） |

【静态·可复算】产出侧链条（`scripts/k4_paired_scenes.py`）：

| 环节 | 位置 |
|---|---|
| 收据 `packages[]` 条目落盘点（携带键 scene/arm/package_id/source_kind/n_techniques/reused） | `k4_paired_scenes.py:262-267` `four["packages"].append(...)` |
| A 臂包计算入口 | `:260` `frozen_package_for_scene(...)`（import 自 `:67` `app.scene_runtime.knowledge_v2`） |
| 收据 JSON 写盘 | `:519` `(d / "k4_paired.json").write_text(...)` |
| 收据读取入口 | `k5_criteria_check.py:107` `latest_k4_artifact()`（探针动态节即用 `build_report(:604)`） |

即：**C3/P2 的「A 臂包」是 K4 运行期经 `query_knowledge`
（`app/knowledge_query.py:365`）查出来、再写进收据 JSON 的结果；
`knowledge_packages` 表本身不是 C3/P2 的直接数据源**。表里零行不影响
C3/P2 取数路径，但 A 臂包是否非空取决于查询资格集合
（`app/knowledge_query.py:35` `ELIGIBLE_STATUS={"verified"}`、
`:74` `ELIGIBLE_OBSERVATION={"observed","replicated"}`）。

【未自跑】动态节（真跑 `build_report` 落 C3/P2 原文）见 §6。

## 3. 结论②：两张表的写入口与「无人工步骤可达性」

【静态·可复算】本工作树全仓 grep（`insert into <表>` /
`KnowledgePackage(` / `StrategyCondition(` / `freeze_package(`，
大小写不敏感）命中分类：

**knowledge_packages 表**
- `app/knowledge_query.py:565` `def freeze_package`——唯一会话级写实现
  （`:580` 构造 ORM 行、`:588` `s.add(row)`）。
- 全仓唯一非测试调用点：`app/scene_runtime/knowledge_v2.py:86`
  `kq.freeze_package(resp, lg_session)`——只在 K3-B 冻结流程
  （runtime 真跑）内可达。
- `scripts/k4_paired_scenes.py:269`、`app/scene_runtime/
  knowledge_v2.py:90`、`app/scene_runtime/knowledge.py:32` 的
  `KnowledgePackage(` 是 **契约对象**（`app/scene_runtime/
  contracts.py:89`，内存包），不写库；探针按 import 来源机械判别。
- `app/models.py:583` 为表声明，非写入点。
- 写路径的人工闸门：`k4_paired_scenes.py` 头注 `:17` 与 `:450-451`
  （`--live` 需 `K4_ALLOW_LIVE=1` 双闸）、`:495` `freeze=a.live`
  ——freeze 写库只随 live 打开。
- **判定：`reachable_without_human=false`**——现无脚本能在无人工步骤
  （live 双闸授权 / runtime 真跑）下对该表产出非空行。

**strategy_conditions 表**
- 非测试写入点：**零**。仅 `app/models.py:494`（表声明）与测试脚手架
  （`tests/knowledge_seed.py:35`、`tests/test_freeze_fingerprint_content.py:85/:146`、
  `tests/test_knowledge_query_v2.py:77`、`tests/test_knowledge_v2.py:101`
  等）add 行。
- **判定：`reachable_without_human=false`**（该表连 gated 写入口都没有，
  条件物化段在生产侧本就无落库路径——这是链上比「写入被闸住」更硬的一段断口）。

## 4. 结论③：门到门链的断段（预期口径）

【主仓记录·转述】`docs/K5晋升链接线_20260925.md`（主控真库实测）：
真卡 8 张全部 `status=hypothesis`（`:25`），`knowledge_packages=0`、
`strategy_conditions=0`（`:24`），机械结论『卡未晋升』（`:27/:32`）。

据此探针在真库上的**应然读数**（本 worker 未复跑，见 §6）：
- 段1 `expression_strategies_v2` 行数=8 → 过；
- 段2 K3 可服务卡=0（无 verified 卡）→ **断**：
  `verdict=卡未晋升`，`stopped_at_segment=K3 eligible_statuses（可服务卡）`，
  后三段不再下结论（撞断即停，不跳段猜）；
- 若未来卡晋升而条件表仍零行 → 断点右移到段3（『K3 过滤』→『写入路径
  缺失』口径已在探针内按序排布，由离线夹具逐态验证，见 §5）。

**纪律重申**：『卡未晋升』只说明缺口在晋升链，**不构成**放松 C3/P2 或
任何判据的依据；探针全文不产 status 变更建议
（`a4.advice_status_change="none"` 硬编码于输出）。

## 5. 结论④：A4（35/48 过门对是否已在库）

- 【主仓记录·转述】48 对成对对照已生成、35/48 过六门这一事实记录在
  旁路账本（`docs/K5晋升链接线_20260925.md:39`）；历轮 K4 均为 dry-run
  （写库需 `--live`+`K4_ALLOW_LIVE=1`，§3），故过门对**未落成
  `strategy_instances` 行**的预期为真。
- 探针口径：`a4_feasible_now = (pairs 文件 48 对可读) ∧ (库内
  extractor_model='paired_contrast_v2' 行数 ≥35)`。任一不满足 → false，
  并把缺项逐条列入 `missing`（文件缺=「不存在/不可读」；行数不足=
  「未入库为行」；账本缺=如实 None+原因，**不猜数字**）。
- pairs 文件（`F:/agi/language-genome/_pairs/...`）与账本
  （`F:/Hermes/team/judge/...`）均在本 worker 可读范围之外（§6），
  两路实测【未自跑】。
- 【静态·可复算】离线夹具已覆盖该判定逻辑：
  `tests/test_k5_promotion_wire_probe.py::test_a4_counts_pairs_ledger_and_db`
  （48 对+35 written 但库 0 行 → false 且 missing 指到「未入库为行」；
  换 40 行的库 → true），
  `::test_a4_files_absent_reported_not_guessed`（文件缺 → None+原因）。

### 探针输出 JSON 键契约（非实跑产物——结构由代码/测试钉死）

```
{probe, task, generated_at, repo_root, db_path, db_available,
 c3_p2_inputs: {static, dynamic},
 writers: {tables: {knowledge_packages, strategy_conditions}, reachable_without_human, note},
 chain: {db_path, read_mode, db_available, db_error, readings, segments[5],
         conclusion: {verdict, reason, stopped_at_segment}},
 a4: {pairs_file, ledger, db, a4_feasible_now, missing, advice_status_change},
 discipline: {db_mode: "ro", model_calls: 0, git_writes: 0,
              verdict_vocabulary, no_status_change_advice: true}}
```
（同构断言在 `tests/test_k5_promotion_wire_probe.py::test_json_key_contract`，
缺键即红。）

## 6. 未自跑清单（每项附原因）

本 worker 会话中**所有**解释器执行与主仓/团队目录读取均被权限层拒绝
（形如 `Error: Allow Bash to run: ...?` / `Error: Allow reading ...?`
的询问在自动判定下即失败；仅 `python --version`、`ls` 获准）。
非脚本缺陷。以下各项需主控在有权限会话复跑：

| # | 未自跑项 | 命令 | 预期（据 §2–§5 静态口径） |
|---|---|---|---|
| 1 | 真库实测（主仓） | `F:/Hermes/hermes-agent/venv/Scripts/python.exe scripts/k5_promotion_wire_probe.py --repo-root F:/agi/language-genome` | `chain.conclusion.verdict=卡未晋升`（stopped 段2）；`writers.reachable_without_human=false`；`a4.a4_feasible_now=false`；`c3_p2_inputs.dynamic` 落 C3/P2 原文 |
| 2 | 门 1：探针本体 | `F:/Hermes/hermes-agent/venv/Scripts/python.exe scripts/k5_promotion_wire_probe.py`（工作树内，无 data/ 库） | exit 0；`chain.db_available=false`→`verdict=证据不足`（不猜） |
| 3 | 门 2：回归 | `F:/Hermes/hermes-agent/venv/Scripts/python.exe -m pytest tests/test_k5_promotion_wire_probe.py -q` | 13 passed, exit 0 |
| 4 | 反向验证（任务书硬性要求） | 把 `chain_segments` 主循环里「`if con is not None and verdict is None`」段的**撞断即停**改为跳过该段续判（如对段2 `passed is False` 时 `continue` 不置 verdict），跑 `pytest tests/test_k5_promotion_wire_probe.py -q` | **至少 `test_stop_at_first_failing_segment` 立即红**（该夹具段2断、段3/4/5全空：跳段会把结论错报成后段断点词）；验后恢复 |
| 5 | C3/P2 动态节单独复算 | `python -c "import sys;sys.path[:0]=['scripts','.'];import k5_criteria_check as k;r=k.build_report(k.latest_k4_artifact(...),...);print([c for c in r['criteria'] if c['id'] in ('C3','P2')])"`（或直接看 #1 报告 `c3_p2_inputs.dynamic`） | 与收据内容一致 |
| 6 | pairs 文件/账本两路 | 见 #1 报告 `a4.pairs_file/ledger` 节 | 主仓路径可读后如实落数 |

### 6b. 主控复跑结果（2026-09-25 23:1x，Hermes 主控当场真跑，替换上表「未自跑」状态）

| # | 状态 | 真跑结果 |
|---|---|---|
| 1 | ✅ 已跑 | exit 0；`verdict=卡未晋升`，`stopped_at_segment=K3 eligible_statuses（可服务卡）`；`writers.reachable_without_human=false`；`a4.a4_feasible_now=false`；段读数 `1:8 → 2:0 → 3:0 → 4:0 → 5:0`（撞段2即停）——与静态预期**逐字吻合** |
| 2 | ✅ 已跑 | exit 0；工作树内无 `data/` → `chain.db_available=false`，`verdict=证据不足`（未猜） |
| 3 | ⚠️ 首跑红 → 修后绿 | 首跑 **exit 1**：`test_a4_counts_pairs_ledger_and_db` 红，真缺陷=`_mk_db` 的 `data.mkdir(exist_ok=True)` 缺 `parents=True`（用例传 `tmp_path/"sec"` 这种不存在的子目录）→ `FileNotFoundError [WinError 3]`。主控改 `mkdir(parents=True, exist_ok=True)` 后 **13 passed exit 0** |
| 4 | ✅ 已跑 | 按 #4 步骤施加跳段变异 → **exit 1，5 条红**（含 `test_stop_at_first_failing_segment`）；恢复后 **13 passed exit 0**，`grep MUTATION`=0 |
| 5 | ✅ 已跑 | 见 #1 报告 `c3_p2_inputs.dynamic` |
| 6 | ✅ 已跑 | 见 #1 报告 `a4.pairs_file/ledger`；`missing=["过门成对对照落库行数=0 <35（35/48 只存在于账本口径，未入库为行）"]` |

报告落盘：`F:/Hermes/team/reports/k5_promotion_wire_probe_20260925.json`。

另一处环境事实：本工作树内**无 `data/` 目录**（真库在主仓
`F:/agi/language-genome/data/`），故 #2 在工作树跑时链节必然走
「库不可读→证据不足」分支——这同时就是离线用例
`test_db_unreadable_is_insufficient` 的被测形态。

## 7. 本 worker 已完成的验证（尽量最大化）

- 探针与测试两文件在盘（工作树内，4 个交付文件之白名单内）；
- §2/§3 全部行号为**本会话当场 grep** 所得（工具输出可复核）；
- §3 的可达性判定逻辑与 `scan_writers` 分类码逐条对读（contract 检测
  用 import 来源正则，已修正多行 import 场景）；
- 只读纪律自查：本 worker 未执行任何写库/写 git/网络动作；唯一落盘
  = 两个交付文档本身；未触碰任何 `data/` 文件。
- 静态复核修复过的缺陷（供会审留痕）：段5谓词元组括号错位；
  「读数不可取」被折叠成缺口态（改为证据不足+不许猜）；contract 类
  误判（多行 import）；`_read` 对非 UTF-8 文件加 `ValueError` 兜底。
