# K2 复审队列：83 条 legacy 实例按新口径（带策略定义）复审的驱动与干跑证据（2026-09-26）

工作树：`F:/agi/_scratch/worktrees/k2-rereview-queue`（分支
`task/k2-rereview-queue`）。纪律：真库只读（mode=ro）、零真实模型调用、
未跑 --live、未改 status/scope 任何一行、未 commit/merge/push。

> **执行环境如实声明**：本任务的命令执行层（Bash 工具）自开工起持续
> 故障（`spawn F:\Git\bin\bash.exe ENOENT`——shell 可执行文件无法生成，
> 重试多次未恢复）。因此**驱动器与回归已完整交付（文件落盘），但
> pytest 验收与真库干跑均「未自跑」**，原因即本条；下文凡未经命令实跑
> 的数字一律标「未自跑」，绝不编造。Shell 恢复后两条命令即可补齐：
> `venv/Scripts/python.exe -m pytest tests/test_k2_rereview_queue.py -q`
> 与 `venv/Scripts/python.exe scripts/k2_rereview_queue.py`（默认只读档）。

## 1. 复审队列缺口（审计 P1 第 3 条，至今未收口）

- 机制侧已修：`app/knowledge_extract.py` 的
  `extract_segment(..., strategy_def=...)` +
  `REVIEW_MARKER_NEW_DEF = "k2def-v1"`（复用
  `strategy_instances.reviewer_version` 列，不新增/不删列）；
  `scripts/k2_extract_backfill.py:574-579` 已传定义正文（四字段：
  abstract_operation / invariants / effect_hypothesis / failure_modes）。
- **队列侧缺口（本任务交付物）**：真库只读实测（任务书已核实的数字）：
  `select coalesce(nullif(reviewer_version,''),'(empty)'), count(*)`
  = **`(empty) 83`** —— 83 条实例一条都没按新口径复审，
  `n_reviewed_new_def = 0`（`KE.review_ledger_stats` 同口径）。
- 缺的驱动器已交付：`scripts/k2_rereview_queue.py`——
  - **默认只读档**：真库 `mode=ro` 枚举 `reviewer_version` 空/None 的
    实例 → 复审队列（逐条 `strategy_id / strategy_key / strategy_version /
    segment_id / work_id / text_version / 现有 status / reviewer_version`）；
    按策略聚合「待复审条数」；逐条校验四字段定义正文非空——拿不到的
    **单列 `missing_strategy_def` 桶**并 fail-closed 计入报告（桶是队列
    分区，`n_queue = n_ready + n_missing_strategy_def`，总数口径不变）；
    缺库/缺表 → 非零退出。
  - **live 档（本任务禁跑）**：双闸 `--live` + `K2_REREVIEW_ALLOW_LIVE=1`
    （缺环境变量 → SystemExit 且未发起任何调用——网关哨兵钉死）；**必须
    `--limit`**（无上限不许起跑）；发起任何调用前打印逐条计划；执行时
    每条 1 次 `KE.extract_segment(..., strategy_def=...)`（定义构造与
    k2_extract_backfill.py:574-579 逐字同源），**只写标记位
    reviewer_version**——不改 status / span / scope 任何一行；新结果与
    既有行的分歧（status/span 不一致）只留痕进报告，绝不自动改原行；
    预算闸复用既有常量 `KE.ExtractBudget()`（max_calls=20 /
    max_tokens=50_000，与 k2_extract_backfill.py CLI 默认同值，不另写口径）。

## 2. 83 条的逐策略聚合表 —— 未自跑（原因见执行环境声明）

驱动器就绪后一条命令即可生成（输出 JSON 的 `by_strategy` 段：
每策略 `pending / missing_def` 聚合 + `queue` 全量逐条 +
`missing_strategy_def` 桶逐条带缺口原因）。**未自跑 ⇒ 本文档不填
任何未经命令实跑的逐策略数字**——83 条总数与 `n_reviewed_new_def=0`
是任务书已核实的实测值，如实引用；每策略分布（按 8 张 legacy 卡的
4~12 条区间，见 strategy_stats 投影历史）**不在此预估**，留待干跑
回填。占位口径（跑后原样替换）：

| 策略卡 | 待复审条数 | 其中缺定义（missing_def） |
|---|---|---|
| （干跑回填：`by_strategy[].pending`） | … | … |

`missing_strategy_def` 桶预期形态（跑后核对）：8 张 legacy 卡的
`effect_hypothesis` 均为非空占位正文（「未定——v1 聚类产物无效果假设
字段」），`abstract_operation / invariants / failure_modes` 亦非空
（docs/策略语义审查清单_20260923.md 可核）——若桶为 0，与「机制侧已修
且定义正文在库」一致；**此为预期核对项，非结论**。

## 3. 复审一条要多少调用 / token（机械推算，既有常量口径）

口径**全部取自既有代码常量**（任务书：不许另写口径）：

| 项 | 值 | 来源 |
|---|---|---|
| 复审 1 条 | **1 次调用**（单实例单调用，重抽同段按带定义新口径） | `extract_segment` 单调用结构（app/knowledge_extract.py） |
| 每调用输出上限 | max_tokens=2000，timeout=60s | `extract_segment` 内常量（app/knowledge_extract.py:174） |
| 每轮预算闸 | max_calls=20、max_tokens=50_000 | `KE.ExtractBudget()` 默认（=k2_extract_backfill.py CLI 默认） |
| 83 条全量 | **83 调用**；默认预算 ⌈83/20⌉=**5 轮**（或显式一轮，--limit/--max 预算同步抬） | 机械算术 |
| token 上界 | 83 × (input + 2000)；最坏输出侧 83×2000=166,000 | 同上 |
| 实测参考（不带定义的历史口径） | ≈256 tokens/调用（12,264 tokens / 48 调用，2026-09-23 backfill live 实测） | docs/HANDOVER 2026-09-23 记录 |
| 带定义推算 | input 增加定义正文长度（四字段，量级数百字符）——**推算非实测**，跑首轮探针后回填 | 本文档口径声明 |

预算闸自洽核对：每轮 20 条 × 2000 输出 = 40,000 ≤ 50,000 ✓（默认轮内
不触顶）。

## 4. 回归（已交付，未自跑——原因见执行环境声明）

`tests/test_k2_rereview_queue.py`（六钉，全离线 raw-sqlite 假库）：
① 只有 `reviewer_version` 空/None 的实例进队列（k2def-v1 的排除）；
② 缺定义正文 → `missing_strategy_def` 单列 + `n_queue = n_ready +
n_missing` 分区口径不变；③ 默认档零调用零写库（只读连接 INSERT 被拒
+ 库文件 sha256 前后不变）；④ 缺库 SystemExit 非零；⑤ `--live` 缺
环境变量 SystemExit 且网关哨兵零命中（未发起任何调用）；⑥ live 缺
`--limit` / 缺 `--extractor-model` 同样先拒。

**验收命令（恢复后补跑）**：
```
F:/Hermes/hermes-agent/venv/Scripts/python.exe -m pytest tests/test_k2_rereview_queue.py -q
```
**未自跑**——Bash 工具 ENOENT（本页开头声明）；不许把「测试文件已写」
说成「验收 exit 0」。

## 5. 未自跑清单（全部标原因）

| 项 | 未自跑原因 |
|---|---|
| `pytest tests/test_k2_rereview_queue.py -q` | Bash 工具 ENOENT，命令无法执行 |
| 真库默认档干跑（83 条逐策略聚合回填 §2 表） | 同上；驱动器就绪，一条命令生成 |
| `--live` 复审（任何条数） | 任务书明令禁跑（无额度拍板）+ 双闸未开 |
| 对既有 83 条 status / scope 的任何写动 | 任务书纪律：禁止改 status/scope 任何一行（驱动器 live 档也只写 reviewer_version） |
